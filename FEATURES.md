# Features

Feature ledger. Grouped by milestone. One line per entry. Plain language.

Legend: ✓ shipped · ⋯ in progress

## Milestone — initial work

### M0 — Local dashboard

- ✓ ratatui app skeleton: header / sessions list / footer, `q` and Ctrl-C to quit
- ✓ local session discovery: scans `~/.claude/projects/`, reads `cwd` from transcript, uses file mtime as last-activity, renders a sortable list with arrow / `j`/`k` navigation
- ✓ live attention state: `notify`-driven transcript watcher derives state from JSONL tail (assistant with `stop_reason == "tool_use"` → working, other assistant entries → needs-input, user/tool result → working), with a 1h idle threshold applied in the UI layer
- ✓ tmux attach: `Enter` switches to the tmux pane whose cwd matches the selected session; `t` opens a new tmux window in that cwd; failures surface in a dashboard status line
- ✓ outside-tmux fallback: when run outside tmux, `Enter` suspends the TUI to run `tmux attach -t <target>` as a foreground subprocess; `t` drops into `$SHELL` in the cwd. TUI resumes when the subprocess exits.
- ✓ resume from transcript: when the selected session has no live tmux pane (e.g. its old window was killed, or there's no tmux server at all), `Enter` spawns a fresh `claude --resume <session-id>` in the session's recorded cwd — new tmux window inside tmux, or new tmux session outside tmux.
- ✓ group-jump keybinds: `J` / `K` jump to the next / previous **project** (lands on the first session of the target group, wraps at the edges, no-op when only one project is on screen). `Ctrl-j` / `Ctrl-k` jump between top-level **sections** with the same semantics. "More modifiers = bigger jump" — j/k single session, J/K project, ⌃j/⌃k section. Helpers live in `dashboard.rs` and are pure index walkers, so the catalog-vs-rows split stays clean. (2026-06-24 dogfood) Section jump now spans the **Favorites** and **Tools** groups too, not just hosts — both render above the host→project tree and were previously reachable only by `j`/`k` paging or the quickswitcher. The shared `walk_to_group` engine treats `FavoritesHeader` / `ToolsHeader` / `HostHeader` as section anchors (`is_section_header`) and lands on each section's first *selectable* row (a tool launch, a favorite or its offline placeholder, or the first session under a host); the `next_host_index` / `prev_host_index` helpers were renamed `next_section_index` / `prev_section_index` to match.

### M1 — Session creation + worktree management

- ✓ minimal config: `~/.config/agent-mux/config.toml` with `workspace_folders = [...]`. Tilde expansion; missing file tolerated (empty list).
- ✓ repo registry: depth-1 scan of each workspace folder at startup, identifying real repos (children with `.git/` as a directory). Worktree pointers are excluded so agent-mux-created worktrees don't litter the picker alongside their parent repos.
- ✓ WorktreeManager: `git worktree add -b <slug> <path> <base>` into a hidden sibling of the parent repo (`<repo>/../.agent-mux-worktrees/<repo>-<slug>`); writes `.agent-mux/task.toml` with task / base_branch / created_at. The dot-prefixed parent keeps the workspace folder uncluttered under default `ls` while `git worktree list` and the dashboard's parent-repo grouping (which reads each worktree's `.git` pointer file) report worktrees correctly regardless of layout. `git worktree add` materialises the intermediate `.agent-mux-worktrees/` directory itself, so no separate `mkdir -p` plumbing is needed. Layout changed 2026-05-18 from a plain `<repo>/../<repo>-<slug>` sibling — dogfooding surfaced workspace clutter outweighing the original file-navigation discoverability rationale, which had already been weakened by the dashboard taking over as the primary discovery surface for worktree sessions. Existing worktrees stay at their original path; only newly-created worktrees land in the hidden dir, and git is happy with both shapes because each worktree's admin entry under `<repo>/.git/worktrees/` uses an absolute path either way.
- ✓ default base-branch resolver: `git symbolic-ref --short refs/remotes/origin/HEAD` → fall back to `main`, then `master`, then prompt with no default.
- ✓ new-session modal: press `n`, pick a repo, name a task, confirm/override the base branch (pre-filled from the resolver). Worktree creation runs on a background thread so the UI never blocks; the footer shows "creating worktree for…" while in flight. On success, `claude` launches in a new tmux window via the existing AttachmentDriver, handing the terminal off via SuspendAndRun outside tmux. **Limitation:** the new session does not appear in the dashboard until agent-mux is restarted — visibility wire-up is tracked as a follow-up in TODO.
- ✓ session titles: each row leads with a human-readable title when available, with cwd dimmed alongside. Precedence: `.agent-mux/task.toml`'s `task` field (for agent-mux-created sessions) → Claude Code's `aiTitle` transcript entry (auto-generated after a few messages) → first user message in the transcript (whitespace-collapsed, truncated to 60 chars with `…`) → fallback to cwd-only. Distinguishes multiple sessions sharing a directory even before `aiTitle` lands. User messages whose content is Claude Code's slash-command wrapper (`<local-command-caveat>…</local-command-caveat>` or `<command-name>…</command-name>`) are skipped at the "first user message" stage — same family as the existing `toolUseResult` skip — so a session whose first input was `/clear` or similar falls through to cwd rather than displaying the envelope text.
- ✓ return-to-dashboard hint: footer keybind line ends with a mode-aware `return: prefix+s` (inside tmux — switch-client took the client to the session's window, `prefix+s` lists sessions to switch back) or `return: prefix+d` (outside tmux — agent-mux suspended itself to run `tmux attach` as a subprocess, `prefix+d` detaches it and the TUI resumes).
- ✓ repo registry TTL refresh: opening the new-session picker triggers a re-scan if the cached snapshot is older than 30s, so repos cloned mid-session appear without an agent-mux restart. The depth-1 walk is cheap enough to run synchronously; the cache prevents repeated walks during rapid open/close.
- ✓ live session discovery: the transcript watcher does a single recursive watch on `~/.claude/projects/` instead of per-file watches, so new transcripts (whether agent-mux just spawned them, or `claude` was started externally) surface in the dashboard as soon as their first JSONL line is on disk. New rows append at the tail so the user's current selection is preserved.
- ✓ stale-session filter: transcripts whose recorded `cwd` no longer exists on disk are dropped at discovery time, so deleted worktrees (e.g. cleaned-up claude-squad worktrees) stop cluttering the list with un-attachable entries. Also filters out the legacy "no cwd metadata" case where the fallback decoded-dir-name path doesn't exist.
- ✓ worktree sessions group under parent repo: discovery reads each surviving `project_dir`'s `.git` pointer file (Git's worktree marker — `gitdir: <parent>/.git/worktrees/<id>`) and records the parent repo path on the `Session`. The dashboard groups by `parent_repo.unwrap_or(project_dir)`, so a session in `discord` and one in a `discord-<task>` worktree share one project header instead of fragmenting into per-worktree entries. Sessions keep their actual `project_dir`; only the grouping key changes. Regular checkouts (where `.git` is a directory) and non-git cwds fall through with `parent_repo = None` and continue grouping by `project_dir` as before. The `.git` read piggybacks on the existing bulk-read pipeline — one extra `Host::read_many` round-trip per discovery pass, amortised across all sessions on the host. `parent_repo` is persisted in the per-host session cache (with `#[serde(default)]` for backward-compat with caches written by earlier builds).

### M2 — Remote hosts

- ✓ host config schema: `[hosts.<name>]` tables in `~/.config/agent-mux/config.toml`, each with `ssh = "<target>"` (a `~/.ssh/config` alias or `user@host`) and an optional `transcript_root` (tilde-expanded; defaults to `~/.claude/projects`). The table key is the dashboard label, separate from the SSH destination so the label can stay friendly. `local` is reserved and rejected at load.
- ✓ `Host` trait + `LocalHost`: read-side host abstraction (list transcripts, read a file, tail N bytes, is-dir) wired through discovery and the transcript watcher. Pure `std::fs` for the local impl; the trait is the seam SSH plugs into without disturbing callers.
- ✓ `SshHost`: shells out to the system `ssh` binary, reusing one `ControlMaster` connection per host so each operation is a local-socket round-trip rather than a full TCP+TLS+auth handshake. Drop guard runs `ssh -O exit`; ControlPersist=600 caps stranded masters at 10 min of idle on the remote side if the guard never fires. `ConnectTimeout=5` bounds the startup hang per host. Remote `find -printf '%T@ %p\0'` for transcript listing, `cat` / `tail -c` for reads, `test -d` for is-dir.
- ✓ remote session discovery: each `[hosts.<name>]` in config gets a background thread at startup that opens its `ControlMaster`, runs discovery against the host's `transcript_root`, pre-computes initial attention against the same connection, and posts results back to the dashboard. Local sessions appear immediately; remote sessions stream in as their hosts connect (parallel, not blocking). Connect failures accumulate into a sticky footer line (`connect failed: name1, name2`) so a second failure doesn't silently overwrite the first; the line sits below transient status. Footer shows "connecting to N host(s)…" while pending.
- ✓ remote attention polling: the Transcript Watcher spins up one background polling thread per connected SSH host, ticking every 3s (M5 will make this configurable). Each tick runs `list_transcripts` on the host's existing `ControlMaster` connection, then `tail -c`s only transcripts whose mtime has advanced since the last scan — idle remote sessions cost one `find` per interval, not N tails. Newly-appearing remote transcripts also surface via this loop, producing the same `NewTranscript` events the local recursive `notify` watch does (tagged with the originating `HostId` so the dashboard builds the session through the right `Host` impl). Result: `needs-input` and `working` transitions on remote sessions reach the dashboard live, within one poll interval.
- ✓ remote tmux attach: pressing Enter on a remote session runs `ssh -S <ctrl-socket> <target> tmux list-panes -a` over the host's existing `ControlMaster`, matches `pane_current_path` against the session's `project_dir`, then `ssh -t <target> tmux attach -t <pane>`. When no pane matches (remote tmux crashed, or the session was started outside tmux), the driver falls through to `ssh -t <target> tmux new-session -A -s agent-mux-<id> -c <cwd> claude --resume <id>` — a deterministic per-conversation tmux session name plus `-A` makes the fallback idempotent (a second attach reuses the same session instead of racing a parallel `claude --resume` on the transcript). Inside local tmux this lives in a new local tmux window (nested tmux — see README for the prefix-collision gotcha); outside tmux it `SuspendAndRun`s the ssh as a foreground subprocess so the user lands directly in the remote tmux. Same shape for `t` (spawn-terminal-in-cwd): `ssh -t <target> tmux new-window -c <remote-cwd>`. The host abstraction exposes the ssh argv builder via `Host::ssh_argv(tty, remote_cmd)`, which shell-quote-joins `remote_cmd` into one final argv element — necessary because SSH joins post-target args with spaces and the remote shell re-tokenizes, which (without quoting) breaks `#`-prefixed `-F` format strings and splits `claude --resume <id>` into three tokens. The `AttachmentDriver` builds the actual `Command`, keeping the spawn/attach concerns out of `Host`.
- ✓ dashboard grouping by host then project: sessions render under bold `── local ──` / `── alpenglow ──` host headers (local always first, SSH hosts alphabetical, headers only appear for hosts with ≥1 session), and within each host under dim project headers showing the home-shortened `project_dir`. Projects within a host order by most-recent-session desc; sessions within a project order by recency desc. `j`/`k`/↑/↓ skip past both kinds of headers — selection always lands on a session row. The per-row host and project spans are gone (the headers carry those signals); session rows are indented two levels and show only the attention glyph, title, and age. Title-less sessions fall back to a short session-id suffix so two unnamed sessions in the same project remain distinguishable.
- ✓ remote-session disk cache: after every successful remote discovery, agent-mux writes a per-host snapshot to `~/.cache/agent-mux/sessions/<host>.json` (id, project_dir, transcript_path, last_activity, title, attention). On next startup, those snapshots seed the catalog *before* any `ControlMaster` handshake runs — so configured remote hosts paint immediately instead of popping in over multiple seconds as each SSH connect completes. When the live discovery for a host returns, `SessionCatalog::reconcile_host` drops entries the remote no longer has, overlays fresh attention/title on the rest, and appends anything new; selection re-seats on the same session id so the highlight doesn't jump. Cache writes are atomic (`tmp` + rename) and best-effort — an unwritable cache dir never fails discovery. Missing or corrupt cache files silently degrade to an empty list. Per-host files keep one bad snapshot from poisoning the others; the wire format is a parallel `CachedSession` struct (not serde-on-`Session`) so in-memory fields can evolve without bumping a schema version.
- ✓ discovery recency filter: `discover()` drops transcripts whose `mtime` is older than `DISCOVERY_MAX_AGE` (30 days, hardcoded for now — M5 exposes the knob) at the `list_transcripts` boundary, before any per-session `cat` / `is_dir` / task-toml round-trip runs. Applied to both local and remote so the user's "old session" mental model doesn't shift based on which host the transcript lives on. Tests pin both halves of the cutoff (cold filtered, warm survives) plus the default-constant path so a future flip in comparison direction blows up loudly.
- ✓ bulk discovery: `Host::read_many` and `Host::is_dir_many` collapse what used to be N sequential SSH round-trips inside `build_session` into one batched call per phase. New shape: (1) list transcripts, (2) bulk-read all surviving transcript content in one round-trip, (3) bulk `is_dir` on the unique `project_dir`s, (4) bulk-read `.agent-mux/task.toml` for the ones that exist. Each phase is one ssh subprocess regardless of N. The fetched transcript content is also reused locally to derive initial attention via `derive_attention_from_content`, so the previously-separate `derive_attention` pre-compute loop in `main.rs` is gone — no extra `tail -c` round-trip per session at startup. SSH `Compression=yes` is set on the `ControlMaster` so JSON payloads ride the SSH gzip layer (no help when the proxy already compresses, no harm otherwise). End-to-end measured 2026-05-18 on a coder-proxied remote with 24 surviving sessions (233 → 24 after recency filter, ~16 MB transcript payload): 7+ minutes (pre-recency-filter baseline) → 55 s (recency filter alone) → ~6 s warm / ~15 s cold (this change). The warm/cold split is the remote VM's page cache; the common re-launch case lives in the warm window. Wire protocol for `read_many` is `OK <bytes>\n<bytes>` per existing file and `MISS\n` per missing path, length-prefixed so per-path NotFound surfaces without failing the batch and binary-clean content round-trips without delimiter games. Tests pin both halves of the parser (mixed-record decoding, truncated-content rejection, bad-header rejection) plus the local-impl parity.
- ✓ progressive remote-host registration: `connect_and_discover` now sends `RemoteDiscoveryResult::Connected` the moment the SSH `ControlMaster` is up (~3 s on a coder-proxied host), letting the main thread register the `Arc<dyn Host>` in `App.hosts` and start both the transcript-polling and pane-polling threads — *before* the slower discovery phase finishes. Previously the host wasn't registered until `Ready` arrived with the full discovered `Vec<Session>`, which meant cached remote rows seeded from `~/.cache/agent-mux/sessions/<host>.json` were *visible* in the dashboard but pressing Enter on them silently no-op'd until discovery completed. Now: cached rows are attachable within seconds of launch; discovery's `Ready` event arrives later and overlays fresh sessions via `reconcile_host` without touching the host registration. `Failed` continues to cover both connect-time failure (host never registered) and discovery-time failure (host stays registered, just no fresh overlay this run). The footer's "connecting to N host(s)…" hint stays up until `Ready` so the user can distinguish "the host is ready" from "discovery still running"; `pending_hosts` decrements on `Ready`/`Failed` only.
- ✓ host write-side primitives (`Host::run`, `Host::write_file`): trait surface for the post-M5 remote-session-creation arc. `Host::run(cwd, program, args) -> io::Result<Output>` is the seam `WorktreeManager` and the default-branch resolver will route their `git` calls through. Returns full `std::process::Output` so callers can dispatch on exit code without a one-size-fits-all "non-zero is Err" baked into the trait — `git symbolic-ref` exiting 1 falls back to `main`, whereas `git worktree add` exiting 1 should propagate stderr. `LocalHost` uses `Command::current_dir`; `SshHost` prefixes the shell command line with `cd <quoted cwd> && ` so the *remote* shell handles directory changes, keeping callers host-agnostic. Each token is single-quoted via the existing `shell_join_quoted` so spaces / globs / leading-`#` (the `-F #{format}` case from `ssh_argv`) round-trip verbatim. `Host::write_file(path, content)` is the write-side counterpart to `read_to_string`; `LocalHost` uses `fs::write`, `SshHost` pipes content over `ssh master 'cat > <path>'`. Not atomic — adequate for the `.agent-mux/task.toml` use case where a retry creates a fresh worktree anyway; an atomic `tmp + rename` shape can drop in later behind the same method when a caller needs durability. No callers switched over yet (pure plumbing); next commit in the arc plumbs `Repo.host` + remote workspace scanning.
- ✓ host-aware repo registry + picker: `Repo` gains a `host: HostId` field so the new-session flow knows which host to create the worktree on. `[hosts.<name>]` config gains an optional `workspace_folders` — when omitted, the host inherits the top-level `workspace_folders` (centralised in `HostConfig::effective_workspace_folders` so the fallback isn't sprinkled across call sites). `RepoRegistry::from_config` scans the local host synchronously at startup; a new `RepoRegistry::reconcile_host(host, repos)` method replaces a host's slice without disturbing other hosts' entries, called after a background workspace scan returns. The scan routes through the trait via `scan_host_workspaces(host, folders)` — one `Host::run("find", …)` per folder to list immediate sub-directories, then one `Host::is_dir_many` batch to filter for real `.git/` directories (worktree pointer files still excluded). Same code path serves local and remote. New-session picker shows host-labeled rows (`[gizmo] alpha  /srv/work/alpha`); rows whose host isn't yet in `App.hosts` (cached / pending / failed) render dimmed and selecting them no-ops, matching the M2 attach UX where cached-but-unconnected rows are visible-but-inert.
- ✓ remote worktree creation through `Host::run`: `WorktreeManager::create` now takes `&dyn Host` and routes every `git` invocation through it (`rev-parse --show-toplevel`, `worktree add`, `show-ref` for the branch-exists probe). `resolve_default_base_branch` likewise. The `.agent-mux/task.toml` metadata write goes through `Host::run("mkdir", &["-p", …])` + `Host::write_file` so the dotfile lands on the right side of the SSH tunnel. `LocalHost::run` now strips the `GIT_*` env vars (`GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_OBJECT_DIRECTORY`, `GIT_COMMON_DIR`, `GIT_PREFIX`) so the worktree tests survive being run from inside a pre-commit hook that has those set against the surrounding repo; SSH hosts don't need this because ssh login shells start with a fresh env. `App::start_creating` resolves the host from `App.hosts` and hands `Arc<dyn Host>` to the background create thread; submitting a remote-repo selection from the picker now actually creates the worktree on the remote (the step-2 "not yet wired" status message is gone). Default-branch resolution for *remote* repos is still skipped at the modal layer — a synchronous SSH round-trip mid-keypress would block the UI — so remote repos start with an empty branch field and the user types theirs; resolving lazily on background-thread is the next polish step.
- ✓ remote `claude` spawn end-to-end: `AttachmentDriver::spawn_session` takes `&dyn Host` and dispatches on `host.id().is_local()`. Local path is unchanged (`tmux new-window -c <cwd> claude` inside tmux, `tmux new-session -c <cwd> claude` outside). Remote path runs `ssh -t target tmux new-session -c <remote_cwd> claude` via the existing `run_remote_interactive` helper that already serves remote attach + spawn-terminal — same inside-tmux / outside-tmux dispatch, same `ssh_argv` plumbing, same `SuspendAndRun` shape for the outside case. No `-A` flag here (unlike the resume path) because this is the initial spawn — tmux auto-names the session and the dashboard will discover the new transcript on the next remote-poll tick, at which point the user can attach via the normal `Enter` path. `NewSessionResult::Created` now carries the `HostId` alongside the path so `drain_creates` can resolve the right `Arc<dyn Host>` from `App.hosts` and hand it to `spawn_session` — the path alone doesn't disambiguate. Completes the four-commit remote-session-creation arc: `n → pick a remote repo → fill task + branch → submit → worktree + branch + task.toml created on the remote → claude launches in a fresh remote tmux session → transcript surfaces in the dashboard → attach via Enter` all without leaving agent-mux.

### M3 — Inline preview

Shipped in two phases (initial `p`-toggled preview pane + the post-M5 `PreviewLine` renderer that fed it), then retired 2026-05-23. The experiment served its purpose — the "see other sessions while interacting with one" signal it surfaced informed the Shape B pivot — and post-pivot it had been made redundant by the embedded-pane attach + sub-second switching combination. Git history holds the implementation if the design choices need to be revisited.

### M4 — Attention notifications

- ✓ OS-level notification on transition into `NeedsInput`: when the catalog moves a session's attention from `Working`/`Idle`/`Unknown` to `NeedsInput`, the `Notifier` fires one notification via `notify-rust` (libnotify on Linux, NSUserNotification on macOS). Payload: title + body (format reworked 2026-07-08 — see the notification-content-overhaul entry below; originally title `agent-mux: <session title>`, body `<host> · <project>`). Dispatch happens on a fire-and-forget thread so a slow D-Bus reply can't back-pressure the UI thread; a missing notification daemon surfaces as a silent no-op (dogfood feedback, not a crash). Wired at the catalog's `update_attention` boundary — the catalog returns the previous attention so the notifier sees a true transition signal, not just a poll-cadence re-derivation of the same state.
- ✓ in-process suppression: two layers per session. (1) Episodic flag — set on dispatch, cleared only when the session leaves `NeedsInput`. One ping per "the assistant just stopped" event; ignoring the notification while the session sits in `NeedsInput` does not produce a second ping. (2) Time-window debounce — `DEBOUNCE_WINDOW = 5s` suppresses re-fires for the same session even when the episodic flag would otherwise allow them. Catches Working↔NeedsInput flapping at the watcher's poll cadence without muting genuine consecutive episodes more than a few seconds apart. Failed dispatch does *not* arm the suppression — a transient libnotify outage stays self-healing.

### M5 — Customization

- ✓ `[notifications]` config section: `enabled` (master toggle, default `true`), `sound` (request the OS default notification sound, default `false` — preserves pre-M5 silent behaviour), `disabled_hosts` (per-host suppression by `[hosts.<name>]` label, default empty). Read at startup into `NotificationsConfig`; `Notifier::update_config` hot-swaps the live config so the M5 reload-on-edit work (still pending) can re-apply without a restart. Leaving-`NeedsInput` bookkeeping runs even while the master toggle is off so re-enabling later doesn't see a stale "still in episode" flag. The dispatcher plumbs `sound` through `Payload` and conditionally calls `notify-rust`'s `sound_name("default")` — picking specific sound files is a post-M5 follow-up.
- ✓ `[theme]` config section: per-element colour overrides for the four attention-glyph states (`needs_input`, `working`, `idle`, `unknown`). Each value is a string: named ANSI colour (`red`, `cyan`, …), `bright_*` variants (`bright_red`), or `#RRGGBB` hex. Empty string or `"default"` falls back to the terminal default with no `fg` set. Unknown colour names fail loudly at config load with the offending field name in the error — silent fallback would mask a typo. Parsing happens once on startup; the resolved `Theme` is a `Copy` struct of `Option<Color>` so render paths never re-parse strings. The attention glyph picks up its theme colour via a small `apply_fg` helper that stays a no-op when the user hasn't themed that element. (Originally also included `tool_use`/`tool_result_ok`/`tool_result_err`/`user_fg`/`assistant_fg` for the M3 preview pane; removed 2026-05-23 when the preview itself was retired.)
- ✓ theme presets: a `preset` field in `[theme]` selects a named built-in palette that the per-element fields then overlay. Ships with `"default"` (attention glyphs uncoloured), `"bright"` (high-contrast `bright_*` variants for every attention state), `"mono"` (no foreground colours at all; structural modifiers like bold/dim still carry the visual distinctions), `"warm"` (sunset reds/ambers/earthy browns), `"cool"` (ocean blues/teals/sea greens; errors stay rose so the attention signal still pops against the cool surround), `"solarized"` (canonical Solarized accents — works over either the dark or light Solarized backgrounds since agent-mux doesn't set a background), `"gruvbox"` (Gruvbox bright variants, earthy retro feel), and `"nord"` (Nord aurora + frost; polar-night slate tones with aurora reds/yellows/greens for events). User-set fields take precedence over the preset's value; un-set fields inherit. An explicit empty-string override clears one preset entry without affecting siblings. Unknown preset name fails loudly at load with the list of available presets in the error. The "default" preset is also what an empty `[theme]` section resolves to, so existing configs render identically.

- ✓ CLI subcommands for discoverability: `agent-mux themes` prints every built-in preset with each colourable element rendered in its actual colour (truecolor `\x1b[38;2;…m` escapes for `Color::Rgb`, named `\x1b[3Nm`/`\x1b[9Nm` for ANSI variants) plus the colour's canonical label so the user can pick a palette by sight before editing the config. `agent-mux config` opens with a "Current configuration" block (which candidate path was loaded, the parsed `workspace_folders` / `hosts` / `notifications` / theme — or the searched-paths list when nothing existed, or the parser error when the file was malformed) and then prints a self-contained TOML reference documenting every key with its default and a one-line description — copy-paste straight into `~/.config/agent-mux/config.toml`. The status block is the diagnostic for "agent-mux isn't seeing my config" without log-spelunking. `agent-mux help` / `--help` lists the subcommands. ANSI escapes are emitted only when stdout is a terminal (via `IsTerminal`), so piping into a non-`-R` pager or a file yields plain text. Unknown subcommand exits with code 2 and prints the help to stderr.

- ✓ Config path resolution honours XDG on macOS: `Config::load` now walks a precedence list (`$XDG_CONFIG_HOME/agent-mux/config.toml`, then `~/.config/agent-mux/config.toml`, then `dirs::config_dir()/agent-mux/config.toml`) and reads the first one that exists. Before, macOS users got `~/Library/Application Support/agent-mux/config.toml` from `dirs::config_dir()` even though every doc, error message, and CLI helper named `~/.config/agent-mux/config.toml` — the documented path silently didn't load. The platform-native location is still searched as a final fallback so anyone already on it isn't disturbed. The candidate list is exposed (`config::config_search_paths`) and fed into `agent-mux config`'s status block so a "no config file found" run names every path that was checked.

### Packaging

- ✓ release-plz-driven tagged releases: `release-plz` (workflow + `release-plz.toml`) reads the conventional-commit log on every push to `main` and maintains a "Release PR" that bumps `Cargo.toml`, regenerates `CHANGELOG.md`, and lists what would ship. Merging that PR creates a `vX.Y.Z` tag and a non-prerelease GitHub release; `release.yml` listens for the `release.published` event and uploads the cross-platform binary tarballs to it. The existing rolling `latest` prerelease still ships on every push for users who want the bleeding edge — the two channels coexist. release-plz uses a fine-grained PAT stored as `RELEASE_PLZ_TOKEN` because the default `GITHUB_TOKEN` doesn't trigger downstream workflows from tag pushes. Pre-1.0 bump behaviour falls out of release-plz's defaults (`feat` and breaking → minor, `fix` → patch); `cargo-semver-checks` is left off until 1.0 is on the table.
- ✓ nix flake: top-level `flake.nix` exposes `packages.<system>.default` (a `rustPlatform.buildRustPackage` over `Cargo.lock`), `apps.<system>.default`, `devShells.<system>.default` (cargo + rustc + clippy + rustfmt + rust-analyzer), and `overlays.default` so consumers can drop `agent-mux` into their `pkgs`. Cross-platform via `flake-utils.eachDefaultSystem` — covers the four common nix systems (`x86_64-linux`, `aarch64-linux`, `x86_64-darwin`, `aarch64-darwin`). Pinning is the consumer's `flake.lock` — `latest`-the-tag staying mutable doesn't matter on this path because the flake input resolves to a commit SHA. Runtime tools (`tmux`, `ssh`, `git`, `claude`) deliberately not in the closure — system-tool dependencies, supplied from the consumer's environment. `doCheck = false` because the test suite shells out to `git`/`ssh` and assumes a real environment (host::tests pin ControlMaster lifecycle); release.yml's release build doesn't run tests either, so the flake matches that split — testing is `check.yml`'s job, the flake just produces a binary. devShell's `shellHook` unsets `DEVELOPER_DIR` on macOS so Apple's `/usr/bin/python3` stub falls back to Command Line Tools instead of the nix-bundled Apple SDK (which has no python3); without this, any python script invoked from a subprocess agent-mux spawns — e.g. the Coder CLI used as a `ProxyCommand` for `*.coder` SSH targets — dies with "tool 'python3' not found" and the remote-host connect exits 255. nix's cc-wrapper has its header paths baked into `NIX_CFLAGS_COMPILE`, so cargo builds (including objc2-* and Cocoa-framework crates) still link cleanly without `DEVELOPER_DIR`. No-op on Linux.

### Post-M5 — embedded-PTY dashboard (Shape B pivot)

The dashboard arc that took agent-mux from "switch-client to a tmux window" to "host a tmux window *inside* the dashboard" — the Shape B transition the `AttachmentDriver` trait was put there to allow. Six standalone phases, each landed as its own commit. The default attach experience changed 2026-05-18; `--no-embed` opts back into the legacy flow for one release.

- ✓ standalone `embedded_pty` module: spawn `argv` into a pty (`portable-pty`), read on a background `std::thread` into a shared `vt100::Parser`, render via `tui_term::PseudoTerminal`, resize the grid, write input bytes. Reader thread is detached so a child that ignores SIGHUP can't stall app shutdown (same shape as the SshHost polling threads). 10 unit tests cover spawn/exit/resize/input-via-pty-echo.
- ✓ `PtyDriver` `AttachmentDriver` impl: emits a new `AttachOutcome::EmbedPty(spec)` variant carrying the argv the embedded widget will spawn. Same try-pane-then-resume dispatch as `TmuxDriver`; the difference is the outcome variant and the fact that PtyDriver always uses the named-`-A` resume form so re-attaches converge. Shared `tmux_attach_argv` / `tmux_resume_argv` helpers consolidate tmux command strings between the two drivers — a future tmux-flag change lands in one place.
- ✓ `Focus` state machine + main-loop wiring: new `Focus::{Sidebar, Terminal{leader_armed}}` enum on `App` routes input. Sidebar focus keeps the legacy keybinds; Terminal focus forwards every key to the PTY via `encode_key_for_pty` (chars + Ctrl/Alt modifiers, Enter/Tab/Backspace/Esc, arrows, navigation keys, F1–F4). `Ctrl-a Esc` is the only intercepted chord — modelled on tmux's prefix convention, picked to avoid collision with tmux's own `Ctrl-b` when nested. Same-session re-attach is a fast path (no respawn); different-session attach drops the old PTY and spawns a new one. Compact ~40-col sidebar layout when attached. Footer collapses to `⌃a esc: back to sidebar` while terminal-focused. Main-loop tick drops from 100 ms (sidebar) to 16 ms (terminal) so PTY output renders snappily.
- ✓ resize cascade: each frame compares the rendered area against `Embedded.last_size` and fires `EmbeddedPty::resize` only on change — sending SIGWINCH on every frame would be 60 wakeups/sec for nothing. Zero-sized areas (terminal shrunk past the borders) are rejected so vt100 doesn't see a zero dimension.
- ✓ mouse forwarding + bracketed paste: `EnableMouseCapture` + `EnableBracketedPaste` enabled at terminal-setup time only when `--embedded` is in effect (non-embedded runs preserve native mouse text-selection). Crossterm mouse events translate from terminal-absolute to PTY-relative coordinates and forward as SGR (xterm 1006) reports — Down/Up/Drag (all three buttons), Scroll{Up,Down}, modifier bits. Paste events wrap in `\e[200~ … \e[201~` with the close-marker stripped from the payload (paste-injection mitigation). Clicks outside the embedded pane are dropped.
- ✓ default flip: `PtyDriver` becomes the default driver; `--no-embed` opts back into the legacy `tmux switch-client` / `SuspendAndRun` path. `--embedded` is silently accepted (no-op) for one release so users who aliased it during the dogfood phase don't see breakage. SPEC.md / ARCHITECTURE.md / README.md updated to describe embedded mode as the default; the TODO entry tracking the arc is deleted.

### Cross-cutting polish

- ✓ session search / filter: `/` opens a search bar in the dashboard that live-filters the session list by case-insensitive substring against title, `project_dir`, and host label — the three things the user reads when scanning. Two modes: Editing (the bar owns the keyboard, characters append to the query, list filters as you type, ⏎ applies, esc cancels) and Active (filter persists but `j/k/⏎/n/t` work normally; `/` returns to Editing with the existing query, esc clears the filter entirely). Selection re-seats on every keystroke so the previously-selected session stays highlighted if it survives the filter, or falls back to the first matching row. Headers collapse when none of their sessions match — `── alpenglow ──` disappears entirely if no remote sessions match, and a project header drops out when all its sessions are filtered out. The list block title swaps from `sessions (N)` to `sessions (visible/N)` while a filter is active so the user knows whether their query narrowed the list before pressing ⏎. Ctrl-C still quits unconditionally even when the search bar has focus.
- ✓ custom tool keybinds: `[[tools]]` entries in `~/.config/agent-mux/config.toml` add user-defined sidebar keybinds that launch a terminal tool in the selected session's cwd. Each entry is `{ key: <single char>, name?: <label>, command: [<program>, <args>...] }`. The `t: terminal` action is the degenerate case (no command — drops into a shell); custom tools route through the same attachment-driver plumbing (`spawn_tool`), so inside tmux they open a new window in cwd and outside tmux they suspend the TUI and run as a foreground subprocess. `{cwd}` and `{host}` tokens in `command` arguments are substituted at fire time against the selected session. Validation at load: single-char `key`, non-empty `command`, no duplicate tool keys, and no collision with built-in dashboard keys (`q j k J K t n / p d` — kept in a `RESERVED_KEY_CHARS` constant alongside `action_for` so a config that tries to override `q` gets a loud error rather than silent shadowing). Works for remote sessions via the same `ssh -t … tmux new-window …` plumbing the existing `t` action uses. `#config #worktree`
- ✓ open a Claude-edited file in your editor (2026-07-08 — coworker request): a `[[tools]]` binding whose command references `{file}` (e.g. `command = ["vim", "{file}"]`) becomes a *file-scoped* tool. Pressing its key on a session row opens an edited-files picker overlay (a quickswitcher sibling — immutable open-time snapshot, no I/O, fuzzy filter, ⏎ opens / Esc cancels) listing every file that session's Claude has edited, most-recently-edited first; picking one substitutes its path for `{file}` and launches the tool through the same `spawn_tool` path a plain launch uses (embedded pane locally, ssh-wrapped for remote — so a remote session opens the editor against the remote path by construction). The list is *transcript-derived*: `derive_edited_files_from_content` pulls `file_path` (or `notebook_path`) from every `Edit` / `Write` / `MultiEdit` / `NotebookEdit` `tool_use` block (`Read` / `Bash` / etc. excluded), deduped and capped at `EDITED_FILES_CAP` (200). Discovery seeds `Session.edited_files` with the full history from the transcript buffer it already reads; the watcher's tail derivation ships recent edits on each `Attention` event and `SessionCatalog::merge_edited_files` unions them in (front for recent/re-edited, older history retained) — no extra read on any path, local or remote. A plain tool (no `{file}`) launches immediately as before; a file-scoped tool on a session that has edited nothing yet surfaces a `no files edited yet` status instead of an empty overlay. `#tools #editor`
- ✓ delete worktree from the dashboard: `d` on a worktree-backed session opens a confirmation modal showing the task / path / host plus a `[ ] force` toggle (`f` to flip). Enter dispatches `git worktree remove` on a background thread via `WorktreeManager::remove` (host-agnostic through `Host::run` — works for local and SSH-reachable worktrees), removes the session from the catalog on success so the row vanishes immediately, and drops the embedded PTY + returns focus to the sidebar if the deleted session was the one being viewed. Pre-cleans `<worktree>/.agent-mux/` before invoking git so the untracked task-metadata file we write at create-time doesn't make every worktree look perpetually dirty — git's safety check only trips on actual user work, which the user then re-runs with the force toggle on. Branch is left alone, transcript is left alone, the remote `agent-mux-<id>` tmux session (if any) is left alone — all three are the user's call to clean up. Sessions whose `project_dir` isn't a worktree (no `parent_repo`) surface a `not a worktree` status line instead of a modal. `#worktree`
- ✓ live tmux-pane indicator: each session row dims its title when no live tmux pane matches the session's `project_dir` — signalling that Enter will fall through to `claude --resume` (multi-second spin-up of tmux + claude) rather than a fast `switch-client` into an existing pane. Mechanism: per-host pane poller spun up alongside the existing transcript watcher (one thread for local, one per connected SSH host) ticks every 3s, runs `tmux list-panes -a -F '#{pane_current_path}'` locally or over each host's `ControlMaster`, sends a `WatcherEvent::LivePanes { host, cwds }` snapshot, and the catalog sets `has_live_pane: Some(bool)` on every session whose `host` matches. Failures (no tmux server, ssh hiccup) surface as an empty `cwds` list — every session on the host transitions to `Some(false)`, matching the user-visible reality. Sessions whose host has not yet reported (initial state, polling thread still spinning up) render at normal weight so a remote whose first poll is in flight doesn't flash dim then bright. The first tick fires immediately so first paint reflects real state. Race against pane death between indicator and Enter is fine: the existing auto-resume fallback handles a stale "live pane" claim gracefully — the indicator is a hint, not a contract.
- ✓ undimmed footer (2026-05-19 dogfood): the footer's status messages and key hints previously rode `Modifier::DIM`, which dogfooding showed was hard to read in practice — the line is informational and the user actually scans it. Status and keybinds share one footer line (status takes precedence when both would otherwise appear), so dropping the dim modifier covers both treatments at once. The broader theme-modifier-override knob under Configuration remains future work; this is the hardcoded fix.
- ✓ new-session picker pre-selects cursor's host/project (2026-05-19 dogfood): pressing `n` from a focused session row now seeds the picker so the cursor lands on the same repo (or at least the same host) rather than always opening at index 0. Implementation: `App::open_new_session` reads the selected session's `HostId` and `parent_repo`-or-`project_dir`, hands it to the modal as `NewSessionSeed`; the modal does an exact host + path match first, falls back to host-only, then to index 0 if nothing matches. Cheap (one linear scan per open) and removes a small but constant per-`n` friction.
- ✓ new-session attach lands in the embedded pane (2026-05-19 dogfood): `PtyDriver::spawn_session` no longer delegates to `TmuxDriver` — it now spawns claude into a detached tmux session (`tmux new-session -d -P -F '#{session_name}' -c <cwd> claude`) and returns an `EmbedPty` that attaches the dashboard's embedded pane to that session. Letting tmux pick the session name guarantees no collision with the user's existing sessions. `drain_creates` synthesises a `SessionId` from the worktree path so `install_embedded` has a key during the brief window before discovery surfaces the real id. Remote symmetry: the same shape over ssh — one round-trip to spawn detached, one ssh+tmux-attach embed. The user now sees claude paint into the right pane immediately rather than getting a jarring fullscreen handoff that broke the post-M5 "sidebar stays visible" contract.
- ✓ no-worktree new-session keybind (2026-05-19 dogfood): `N` (Shift+n) opens the same picker as `n` but in `ModalMode::NoWorktree` — picking a repo immediately spawns claude in the repo root rather than running `git worktree add` first. No task name, no base branch, no `.agent-mux/task.toml`. The modal title swaps to `new session (no worktree): pick a repo` so the mode is visible. Reuses the existing create-channel pipeline (`drain_creates`) by pushing a synthetic `Created { host_id, path: repo.path }` directly — no background work to surface, no `creating` footer state. Works for both local and remote repos through the same `spawn_session` path that worktree-backed sessions use. `N` joins `RESERVED_KEY_CHARS` so user-configured `[[tools]]` bindings can't shadow it.
- ✓ top-level `workspace_folders` rejects tildes (2026-05-19 dogfood): top-level entries used to be tilde-expanded against the local user's home at config load — fine for local-only setups but silently broken for the inherit-to-remote case where the same absolute path got shipped to a remote box that uses a different username. Now `Config::load_from` errors loudly with a directive ("use an absolute path, or move the entry under [hosts.<name>].workspace_folders"). Per-host `workspace_folders` still accept tildes — they pass through to the remote shell which expands them against the remote user's home, the documented and correct behaviour for that section. One rule per section, both unambiguous; no path-rewriting heuristics needed.
- ✓ empty-scan footer hint (2026-05-19 dogfood): when a remote workspace scan completes with zero repos, the footer now surfaces a sticky line — single-host as `alpenglow: no repos in /home/gizmo/workspace`, multi-host as `no repos found: alpenglow, beta`. Slot sits one tier below `connect failed:` in the precedence ladder (a host that connected-but-empty is less severe than one that didn't connect at all). The signal was the diagnostic gap that made the inherit-to-remote tilde bug so hard to find; before this, a configured-but-broken remote path was indistinguishable from "host still connecting." Subsequent non-empty scans clear the warning for that host.
- ✓ config reference moved from `agent-mux config` to `agent-mux help` (2026-05-20 dogfood): the long TOML skeleton printed by `config` was burying the live-parsed status (the actual diagnostic value of the subcommand — "did you read my file?"). Reference is now a one-screen compact form at the bottom of `agent-mux help`: every section, the theme field list, the preset names, the [[tools]] shape — but rendered as descriptive lines rather than a copy-paste TOML skeleton. `config` is now status-only.
- ✓ `agent-mux themes` renders as a table (2026-05-20 dogfood): replaced the per-preset block layout (8 presets × 7 element rows ≈ 90 lines) with a single grid — rows are presets, columns are elements (`● needs_input`, `◐ working`, …). Each cell is the colour name the user would write in `config.toml`, rendered in that colour when stdout is a TTY. Uncoloured cells show an em-dash to flag "no override" without occupying the wide "(terminal default)" phrase the old layout used. Column widths are computed at render time off the actual values, so a future preset with a longer colour string can't silently truncate. Output is now ~22 lines.
- ✓ fix(attachment): remote `t` and `[[tools]]` no longer fire-and-forget into the void (2026-05-20 dogfood): pressing `t` (terminal) or a custom tool keybind on a *remote* session used to send `tmux new-window -c <cwd>` *to the remote tmux server* — which landed the new window in whatever session was "current" on the remote (not necessarily the one the user was attached to), while the *local* wrapper window died immediately because the ssh command (`tmux new-window`) returned 0 the moment the remote tmux took it. Net effect: a dead local window plus an invisible remote window. Now: both remote spawn paths ssh into the host with `sh -c 'cd <cwd> && exec <shell-or-tool>'`. The local window (or `SuspendAndRun` subprocess) hosts the remote shell or tool directly and stays alive until the user exits it — the same shape local-in-tmux already had. Paths with spaces survive both quoting layers; new regression tests pin the wire shape against future drift.
- ✓ `t` and `[[tools]]` open in the embedded pane (2026-05-20 dogfood): `PtyDriver::spawn_terminal` and `spawn_tool` used to delegate to `TmuxDriver` (sibling local tmux window — which dogfooding confirmed was effectively a fullscreen handoff because tmux switched the local terminal to the new window and the dashboard view disappeared). Now both methods emit `EmbedPty` for both local and remote, so pressing `t` or a `[[tools]]` keybind swaps the embedded pane to a shell/tool at the session's cwd. Sidebar stays visible; pressing Enter on the session row swaps back to the claude attach. Synthetic SessionIds (`agent-mux-terminal:<sid>`, `agent-mux-tool:<idx>:<sid>`) key the pane swap so the same keybind refocuses an existing pane while different keybinds cycle freely. The whole "embedded pane is the single 'doing stuff' area" mental model now applies to every action — `Enter`, `n`, `N`, `t`, and tools all land there.
- ✓ focused-pane border indicator (2026-05-20 dogfood): in embed mode the sidebar and the embedded terminal each carry a focus-aware outer border — the focused side renders cyan + BOLD, the unfocused side stays DIM. Replaces the prior BOLD-vs-DIM-only treatment that dogfooding showed was too subtle to spot at a glance, so users no longer mis-target keystrokes (stray edits into the inner PTY, or dashboard commands swallowed by Claude's composer). Outside embed mode the sidebar's border stays plain because there's no rival pane to disambiguate against. Border colour is hardcoded to cyan for now; a `[theme.focus_border]` knob is filed under the broader `[theme]` schema extension in TODO.
- ✓ live last-activity cell on active conversations (2026-05-20 dogfood): before, the sidebar's "Xm ago" column was set once at discovery from the transcript file's mtime and never refreshed — an actively-running conversation would display 38m+ ago despite being mid-conversation because the catalog field never moved. The transcript watcher's `Attention` events now carry the freshest mtime they observed (poll path captures `stat.mtime`, local notify path stats the file alongside the tail read, initial-prime sends `None` to avoid touching the no-op case), and the main loop calls a new `SessionCatalog::touch_activity` to bump the field only if the proposed value is *newer* — so out-of-order races between the poll-tick and the notify event can't rewind the cell. Result: an active conversation reads near-zero, idle ones still age naturally toward the 1h idle threshold.
- ✓ per-session tmux pane resolution (2026-05-20 dogfood): two bugs landed together — distinct sidebar rows attached to the same tmux pane when the rows shared a `project_dir`, and the per-row dim/un-dim "live pane" indicator bled across all sessions in a project. Shared root cause: `find_pane_local` / `find_pane_remote` matched on `pane_current_path` only, and `apply_live_panes` set `has_live_pane` from the same cwd-only set. Both surfaces now use a deterministic name first: the resume fallback's `agent-mux-<session.id.0>` tmux session name pins each session to its own pane independently of cwd. `find_pane_local` falls back to cwd matching for externally-created or never-re-attached sessions (no regression there); `apply_live_panes` likewise treats the cwd as a secondary signal. Pane poller's `list_live_panes` now returns both cwds and session names from a single tmux call (`#{session_name}\t#{pane_current_path}`), so the cost is unchanged. Externally-created sessions sharing one cwd will still collide on the cwd fallback; that limitation is documented in code and won't surface for agent-mux-spawned sessions.
- ✓ leader-chord Esc is lenient about modifier bits (2026-05-20 dogfood, defensive): the embedded-pane leader-armed Esc transition used to require `key.modifiers.is_empty()`. Under Kitty Keyboard Protocol or extended event modes Esc can arrive with stray modifier bits, and the strict check let those events fall through to the "forward both" branch — silently keeping focus in Terminal while forwarding the user's subsequent navigation + Enter into the inner PTY. The match now accepts Esc with any modifiers, which is safe because `Ctrl-a Esc` to the inner tmux isn't a meaningful sequence. Leading hypothesis for the "stray newline on session swap" dogfood bug; the TODO entry stays open with a "re-open with byte-level evidence" instruction in case the repro persists.
- ✓ Shift+drag bypasses embedded-pane mouse capture (2026-05-20 dogfood): dragging over the embedded terminal used to produce no selection — mouse capture forwarded the drag to the inner PTY, suppressing the host terminal's native selection. agent-mux now drops any mouse event with `Shift` held, matching the iTerm2 / kitty / wezterm / Alacritty convention; the host terminal does native text selection and the inner PTY never sees the event. Most of those emulators already implement the bypass at the OS level (we never receive the event), so this is a belt-and-braces guarantee for the ones that forward Shift-mouse anyway. Over-SSH selection and host terminals without an OS-level Shift bypass are filed in TODO under "in-app copy mode + OSC52".
- ✓ mouse-wheel scrollback documented (2026-05-21 dogfood): SGR wheel events were already being forwarded into the inner tmux via `encode_mouse_event`, but tmux only acts on them when its own mouse mode is enabled — and without that, the wheel passed through to Claude Code, which has no scrollback of its own. The original TODO suggested agent-mux maintain a Rust-side history buffer; closer inspection showed tmux already does this work better, and the only missing piece was a one-line README note telling users to `set -g mouse on`. Documented in the README's "Embedded pane" section.
- ✓ attention-heuristic audit (2026-05-21 dogfood, no behavioural change): read-only audit of three smells flagged after the per-row bleed fix landed in item 3. Findings — (a) "working" doesn't linger pathologically: `derive_attention_from_content` correctly returns `Working` exactly when the last classified entry is a user message or tool result, which is the truth about Claude having more work to do. (b) The documented 1h idle threshold is enforced by `effective_attention` as a strict greater-than, so a session at exactly 1h reads as still-active. (c) Local notify and remote poll both route through the same `derive_attention` function; the only difference is latency (millisecond local vs 3s remote poll cycle) — no divergence at the derivation layer. Locked the boundary with three regression tests on `effective_attention` (under-threshold, just-under, past-threshold) so a future tweak to `>=` blows up loudly.
- ✓ user-editable session names (2026-05-21 dogfood): pressing `r` on a session row opens an inline rename overlay in the search-bar slot. Type to edit, ⏎ commits, esc cancels; committing an empty string clears the override. Overrides persist to `~/.cache/agent-mux/session_names.json` (atomic `tmp + rename` writes, malformed file degrades to empty store) keyed by `(host_id, session_id)` so two sessions with the same id on different hosts hold independent renames. Overrides take precedence over the transcript's `aiTitle` / `task.toml` title — once the user named something, they meant it; a later-arriving AI title does *not* clobber the rename. `r` joins `RESERVED_KEY_CHARS` so user-configured `[[tools]]` bindings can't shadow it.
- ✓ tools-launch registry surfaces re-attachable tool sessions (2026-05-21 dogfood): pressing a `[[tools]]` keybind on a session row now wraps the tool command in a detached tmux session (`tmux new-session -d -P -F '#{session_name}' -c <cwd> <joined>`) and registers the assigned name in a new `ToolLaunchRegistry`. Sidebar renders a `── tools ──` group at the top with one row per running launch (`⚒ name [host] Xs ago`); Enter on a tool row re-attaches the embedded pane to that tmux session via `tmux attach -t <name>` (or `ssh -t target tmux attach -t <name>` for remote tools — host-aware from day 1). Lifecycle: registry entries prune automatically when the inner PTY exits (the embedded `Embedded` struct now carries an optional `tool_tmux_session` that `drain_pty_events` checks before clearing). No background poller — failure surfaces at the next interaction, matching the "fail loudly at the next user touch" discipline. Closes the 2026-05-20 dogfood signal where a launched tool kept running invisibly until the user `tmux ls`'d and attached by name.
- ✓ per-platform notification backends (2026-05-21 dogfood): the M4 notifier originally dispatched through `notify-rust`, which 2026-05-20 dogfooding confirmed was unreliable on both the user's macOS (NSUserNotification handoff failed silently) and WSL setups (D-Bus fragile under WSLg). The `Dispatcher` trait now has three platform-aware impls: `LibNotifyDispatcher` (Linux desktop via notify-rust), `OsascriptDispatcher` (macOS, shells out to `osascript -e 'display notification …'` with no daemon dependency), and `WslToastDispatcher` (WSL, shells out to `wsl-notify-send.exe`). `pick_dispatcher(backend)` resolves a new `[notifications] backend = "auto" \| "dbus" \| "osascript" \| "wsl-toast"` config value into the concrete dispatcher plus a short label, and main logs that label to stderr at startup so a misfiring auto-probe is visible in shell scrollback rather than silently muting the user. Auto-probe rules: cfg!(target_os = "macos") → osascript; `/proc/sys/kernel/osrelease` contains "microsoft" or "wsl" → wsl-toast; otherwise → dbus. Each dispatcher still spawns a one-shot thread per dispatch, so the OS handoff latency stays off the UI thread.
- ✓ attention heuristic respects `stop_reason` (2026-05-21 dogfood): the original rule "last assistant entry in transcript → NeedsInput" flickered every agentic-loop tool call through NeedsInput, because the assistant's `tool_use` entry briefly *was* the last entry before the matching `tool_result` arrived. The per-session debounce window absorbed most rapid-fire repeats but the *first* fire in each window still landed, so a long session with many tool calls produced steady notification noise. `derive_attention_from_content` now inspects `message.stop_reason` on assistant entries: `tool_use` maps to `Working` (the assistant is awaiting a tool, not a human); `end_turn` / `stop_sequence` / `max_tokens` / missing / unfamiliar values map to `NeedsInput` (conservative — an unknown shape stays loud rather than silently quiet). The Claude Code `Notification`-hook integration was researched as a richer signal source but deferred (filed under TODO) — order-of-weeks of new subsystem work vs order-of-hours for the `stop_reason` refinement that captured the dominant noise source.
- ✓ hook ingress filters by `notification_type` (2026-05-21 dogfood): the phase 1 ship treated every `Notification` event as `NeedsInput`, which fired on every tool-completion event the user saw — the user reported "notifications fire any time a tool finishes." `receive_hook_from_stdin` now parses the payload's `notification_type` field and only writes a marker for `permission_prompt`, `idle_prompt`, or `elicitation_dialog`; `auth_success`, `elicitation_complete`, `elicitation_response`, and any unrecognised future type are logged to stderr and dropped. A missing `notification_type` falls back to writing the marker (conservative — unknown payload shapes stay loud rather than silently quiet, matches the `derive_attention_from_content` fallback pattern). Every event logs its `notification_type` to stderr regardless of outcome, so the user can see in shell scrollback what Claude Code is actually emitting on their setup — the lever for refining the allowlist over time. Tests pin each allowlist entry + each filtered type + the missing-field fallback + the unrecognised-future-type fallback.
- ✓ `agent-mux install-hooks` writes the Notification-hook entry into `~/.claude/settings.json` (2026-05-21 dogfood — paired with the phase 1 ingress below): one-shot setup so users don't have to hand-edit JSON. `plan_install` is a pure function (input: current settings string + binary path; output: merged JSON + action enum) so the JSON-merge edge cases — empty file, file with unrelated settings, file with other Notification hooks, file with our entry already, file with a stale agent-mux path — are unit-testable without filesystem fixtures. The CLI wrapper `install_hooks_at` does the I/O: one-time backup to `settings.json.bak` (only if no backup exists yet), atomic write via `tmp + rename`, post-write re-parse to verify the result is valid JSON. Identifies "our" entries by splitting the command on whitespace and matching first-token-basename == `agent-mux` AND second-token == `hook`, which catches every shape we write (`/abs/agent-mux hook`, `agent-mux hook`) without false-matching unrelated commands that happen to mention agent-mux in flags. Idempotent: same-path entries no-op; stale-path entries update in place with a printed notice; entries from other tools (different commands) coexist alongside ours. `--dry-run` flag prints the planned content without writing. Binary path comes from `std::env::current_exe()` so the installer writes the absolute path to whichever build just ran.
- ✓ `Notification` hook ingress, remote sessions (2026-05-21 dogfood — phase 2 of the hook integration arc): the phase 1 ship was local-only because the hook marker would land on the remote box, out of reach of the local file watcher. Now: the `agent-mux hook` subcommand derives the target dir from the payload's `transcript_path` field (`<root>/.agent-mux-hooks/`) — same path shape on local and remote, so a single subcommand impl works identically on whichever machine `claude` is running on. The local `init_hook_watcher` now watches `<projects-root>/.agent-mux-hooks/` instead of the previous `~/Library/Caches/agent-mux/hooks/`. Two new `Host` trait methods support the remote ingest: `list_files(dir)` (flat directory listing, GNU `find -maxdepth 1 -type f -print0` for SSH hosts), `remove(path)` (single-file delete, `rm -f` on SSH so a re-run is idempotent). The existing per-host transcript polling thread gets a sibling tick `poll_hooks_once` that drains `<remote-root>/.agent-mux-hooks/` over the same `ControlMaster` connection — no new threads, no new SSH masters, latency bounded by the existing `REMOTE_POLL_INTERVAL` (3 s). Bad payloads stay on the remote for inspection; `.tmp` artifacts from a producer mid-write are skipped on both ends. Setup is one command per host: ssh in, `agent-mux install-hooks` (the installer already worked on remotes because it uses `current_exe()`; this commit makes the ingress symmetric on the other side). Tests pin the per-tick remote ingest (event-per-marker + delete-after-send, .tmp exclusion, missing-dir tolerance, bad-payload preservation, receiver-dropped exit). The cache-dir fallback in `fallback_hook_dir` is preserved for malformed payloads missing `transcript_path` (production payloads always include it; the fallback exists for hand-crafted callers and smoke tests).
- ✓ Claude Code `Notification` hook ingress, local sessions (2026-05-21 dogfood — phase 1 of the hook integration arc): closes the regression where assistant `tool_use` entries with pending permission prompts stayed silent (the `stop_reason` fix earlier the same day mapped tool_use → Working, which is right for tools that auto-execute but wrong for those that block on user approval — the transcript can't tell those cases apart). The Notification hook fires exactly when Claude Code needs user attention, with no heuristic ambiguity. New surface: (a) `agent-mux hook` CLI subcommand reads the hook JSON from stdin and writes a marker file (atomic `tmp + rename`) to `~/Library/Caches/agent-mux/hooks/<unix-millis>-<session_id>.json`; (b) `hook_ingest::spawn_hook_watcher` runs a `notify`-backed watch on that directory in the dashboard process, emitting `WatcherEvent::Hook { id, received_at }` for each new marker (with a startup sweep to drain any markers that landed while the dashboard was offline); (c) `SessionCatalog::apply_hook_event` forces the session to `NeedsInput` and pins `hook_pinned = received_at`; (d) heuristic-derived attention updates route through a new `apply_heuristic_attention` that suppresses updates whose mtime is `<=` the pin (so the heuristic can't downgrade a hook-triggered NeedsInput back to Working while the prompt is still pending), automatically clearing the pin once the transcript mtime advances past it. Setup is one edit to `~/.claude/settings.json` documented in README (matcher `.*` so we ingest every Notification event regardless of matcher type — the matcher-specific field names from Claude Code's docs weren't independently verified during research and the integration is robust against future schema changes by only depending on `session_id`). Deliberately out of scope for phase 1: remote sessions (the hook runs on the box where claude runs — for remotes the marker would land on the remote and need to come back across SSH; planned via writing to the remote's `transcript_root` so the existing poller picks it up), auto-installing the hook into `settings.json` (filed in TODO), and matcher-specific behavior. Tests pin the catalog override semantics (suppress when stale, clear pin when transcript advances, idempotent across rapid hook fires) and the hook ingest module (subcommand round-trip, marker filename format, atomic write, startup sweep, .tmp exclusion, out-of-dir defence, bad-payload preservation for inspection).
- ✓ notification suppression when actively viewing the session (2026-05-23 dogfood, expanded same day): the M4 suppression layers caught "the user has seen this NeedsInput episode" via the episodic flag, but not "the user is literally watching this conversation right now" — embedded-pane attach + keyboard focus on the terminal still produced an OS toast for a transition the user could see in real time. New `Transition::actively_viewed` flag, set at the `main.rs` wire-up site, causes the notifier to skip dispatch *without* arming the episodic flag — so a later transition (user replies, Claude works, stops again) while focus has moved elsewhere still fires normally. The predicate evolved twice the same day: the first cut was `Focus::Terminal { .. } && embedded.session_id == transition.id`, which dogfooding immediately showed was over-aggressive — an alt-tab to a browser window left the in-app `Focus::Terminal` state intact, so the user missed real attention transitions while the terminal sat unfocused in the background. Fixed by adding a third condition: agent-mux now enables DEC 1004 focus reporting (`crossterm::event::EnableFocusChange`) on terminal setup, tracks `App.terminal_focused` from `Event::FocusGained` / `Event::FocusLost`, and ANDs that into the predicate. `terminal_focused` defaults to `false` so terminals that don't implement focus reporting silently fall back to "always notify" — the safe failure mode (missed suppression is annoying; missed notification is the bug we exist to avoid; a real focus-supporting terminal sends `FocusGained` on startup, lifting the field to `true` within the first event tick). Pinned by two regression tests on the notifier: suppressed-when-active, and a fire-on-next-non-active-transition test that nails down the no-episodic-arm semantics. The terminal-focused integration itself is exercised by the live event loop rather than unit tests — there's no headless way to drive a focus event through crossterm without a real terminal.
- ✓ movement keybinds relocate from footer to sidebar title bar (2026-05-24 dogfood): the footer line had been growing dense since the 2026-05-17 group-jump hints landed; the same-day favorites ship pushed it over because `r: rename` was already unsurfaced and `f: favorite` had no place to land either. Pure-movement triplet (`j/k`, `J/K`, `⌃jk`) moved into the sidebar's top title bar via a new `compose_sidebar_title(catalog_len, visible, search_active)` helper — glyph-only abbreviation (no `: move` / `: project` / `: host` labels) so the 40-column embedded sidebar still fits comfortably for counts under 1000. Footer now carries action verbs only (`⏎/t/n/N/r/f/d/q`) plus tool bindings, return-to-tmux hint, and pending-host status. Pinned by an embedded-budget regression test (`sidebar_title_stays_within_embed_sidebar_budget_for_typical_counts`) that asserts the typical-case title fits in ≤36 cols so a future format tweak silently overflowing into truncation gets caught; plus complement tests for movement-hints-gone-from-footer and `r: rename` / `f: favorite` now present.
- ✓ user-pinned session favorites (2026-05-24 dogfood, spec resolved same day at TODO.md:82). Motivating pain: `build_display_rows` orders projects by `max(last_activity) desc`, so frequently-attended conversations never settle into a stable position the user can muscle-memory — the activity-driven reshuffle keeps moving them. The pinned `── favorites ──` group at the very top of the sidebar decouples that subset from the reshuffle. Per-`(host, session_id)` keying mirrors `SessionNameStore`; persistence to `~/.cache/agent-mux/favorites.json` uses the same atomic tmp+rename + malformed-degrades-to-empty contract. `f` toggles favorite on the selected row (added to `RESERVED_KEY_CHARS` so `[[tools]]` bindings can't shadow it; modal-local `f` in the delete-worktree modal still flips the force toggle because the modal handler intercepts keys before action dispatch). Render: pinned `── favorites ──` BOLD header above all hosts, sessions inside the group at 1-level indent with a `★` glyph and an inline `[host]` suffix (the natural tree's host header doesn't apply across the pinned group's host-spanning recency-desc order). Favorited sessions appear *twice* in the row list — once in the pinned group, once in their natural host/project group with a `★` glyph — so neither view loses information. `DisplayRow` gains `FavoritesHeader` and `FavoriteSessionRow(usize)`; `build_display_rows_filtered` takes a new `is_favorite` predicate alongside `include`; both predicates compose so the favorites group obeys the search filter (asymmetric "favorites always show" surprises users more than it helps per the resolved spec). Selection re-seat learned about the duplication: new `SelectionAnchor { id, in_favorites }` captures which copy the cursor was on, and the pure `pick_reseat_target` helper prefers the same kind, falls back to either kind, then to `first_session_index` — without this a `position(|r| matches_session_id)` would silently jump the highlight to the favorites copy on every catalog drain. Pinned by 9 favorites-store tests, 5 dashboard tests (section appears + omitted + recency ordering + obeys search + indices point at original slice), and 6 `pick_reseat_target` tests (same-kind preferred, either-kind fallback, anchor-gone fallback, no-prior seed, empty list). Out of scope per spec: project-level favorites, manual reorder within the group, notification priority tie-in, cross-machine sync.
- ✓ sidebar selection survives activity-driven row reshuffles (2026-05-24 dogfood): `build_display_rows` orders projects by `max(last_activity) desc`, so an attention update on the currently-active conversation (every tool_use, every NeedsInput, every transcript write) can bump its project group up the list and reshuffle row indices under the cursor. `list_state` held an absolute row index — without a re-seat, the highlight stayed on the old index, visually jumping onto a different row (often the project header, which then read as "the project title disappeared" while the highlight landed on an inactive chat). `App::drain_updates` and `App::drain_deletes` now mirror the capture-prior + reseat-after pattern the remote-discovery reconcile path already used (`reseat_selection_to`), pinning the highlight to the same *session id* across the drain. Falls back to the first session row when the prior id is gone (delete consumed it). Underlying ordering invariant is pinned by `dashboard::projects_order_by_their_most_recent_session`; the re-seat helper itself is exercised by the four existing callers (search exit, search edits, remote reconcile).
- ✓ notification titles honour the user's rename override (2026-05-24 dogfood): the rename feature shipped 2026-05-21 promised that overrides took precedence over the transcript's `aiTitle` / `task.toml` title "once the user named something, they meant it" — but only the *sidebar* render consulted `SessionNameStore`. The notifier path in `App::fire_attention_notification` skipped straight to `session.title`, so a session the user had renamed kept surfacing the transcript-derived title (often the first user-message line, which read as the "original commit title") in every OS toast. The title-resolution chain is now lifted into a pure `resolve_notification_title(name_override, catalog_title, session_id)` helper next to `compute_actively_viewed` (same regression-test rationale) and consulted from `fire_attention_notification`; precedence matches the sidebar's `format_session_row` exactly — override → catalog title → trailing six chars of the session id. Pinned by five precedence tests on the helper.
- ✓ belated toast on focus-loss for actively-viewed suppression (2026-05-24 dogfood): closes the false-negative left by the `actively_viewed` suppression above. Scenario the user surfaced: they were watching a session's embedded pane when it transitioned to `NeedsInput` (a `permission_prompt` from the Notification-hook ingress), then alt-tabbed to their browser — and never heard about the prompt, because the transition-time suppression was correct but produced no follow-up signal when window-manager focus left the terminal. The notifier now stashes the would-be `Payload` on the per-session state whenever `actively_viewed` causes suppression, and a new `Notifier::on_terminal_focus_lost` entry point — called from the `Event::FocusLost` branch of the main loop, alongside the existing `terminal_focused = false` flip — drains pending payloads and dispatches the belated toasts. The pending slot is cleared when the session leaves `NeedsInput` (the trigger no longer applies, so we don't surface a stale toast for an episode that's already over) and a dispatch failure leaves the payload in place so the next focus-loss edge retries (matching the rest of the notifier's "transient backend outage shouldn't silently swallow a user-attention signal" stance). The original layered suppression invariants still hold: the existing time-window debounce gates belated toasts too (so a normal fire + re-entry + focus-loss within 5 s doesn't double-ping), and a successful belated dispatch arms `fired_for_current_episode` so subsequent `NeedsInput→NeedsInput` catalog reasserts don't refire. Important scoping caveat: this fix is only effective when the terminal actually emits DEC 1004 `FocusLost` — a terminal that sends `FocusGained` on startup but never `FocusLost` (some WSL2 terminals exhibit this, depending on host/server combinations) leaves `terminal_focused` stuck at `true` and suppresses every `actively_viewed` transition without ever delivering the belated toast; that's the same failure shape the original `actively_viewed` predicate already had, surfaced more visibly here. Pinned by six new notifier tests: belated fire on focus-loss, no-op when nothing is pending, no refire after the session leaves `NeedsInput`, episodic-flag arming after belated fire (so catalog reasserts don't double-ping), dispatch-failure leaves the pending payload in place for retry, and debounce-window suppression against a prior fire.
- ✓ WSL notification ASCII-fold (2026-05-23 dogfood): the per-platform notification backends shipped 2026-05-21 confirmed that `wsl-notify-send.exe` surfaces toasts reliably on the user's WSL2 + WSLg setup, closing the M4 dogfooding gap — but the verification toast revealed that non-ASCII characters in title/body arrived as mojibake. The production body format `format!("{} · {}", host, project)` uses U+00B7 middle-dot as a separator, so *every* real notification was hitting this. Root cause: WSL→Windows argv conversion through NT's wide-arg layer is lossy under the active console code page, so the middle-dot (and any em-dash / smart quote / ellipsis a user puts in a task title) arrived at `wsl-notify-send.exe` as the UTF-8 bytes interpreted under CP-1252 — `·` rendered as `Â·`, `…` as `â€¦`. Fix: a small `ascii_fold` helper applied only inside `WslToastDispatcher::dispatch` (and `dispatch_blocking`) — the macOS osascript and Linux D-Bus paths keep their pretty Unicode untouched. The fold maps the typographic offenders most likely to appear in a task name (em/en dash → `-`, smart quotes → straight, middle-dot → `|`, ellipsis → `...`) and degrades unknown non-ASCII to `?` rather than smuggling raw bytes that would render as garbage anyway. Also caught during this pass: `wsl-notify-send`'s flag semantics are *empirically* "category = toast title slot, sole positional = body slot," despite `--help` describing `--category` as a notification category tag and implying a positional summary; passing two positionals makes the binary print its usage and exit non-zero, which the spawned thread silently swallows. The original `--category <title> <body>` argv shape was therefore correct, just under-documented — the dispatcher now carries a comment pinning that finding so a future "this looks wrong, let me fix it" pass doesn't repeat the same near-miss. Pinned by a unit test covering pure ASCII passthrough, each of the named mappings, and the catch-all fallback.
- ✓ custom notification sound files + `agent-mux notify-test` (2026-05-21 dogfood): the M4 ship's only audio knob was `sound = true/false`, mapping to the OS "default" sound — which on macOS is aggressive enough that the user kept the toggle off altogether (losing the audible cue entirely). New `[notifications] sound_file = "<absolute-path>"` field accepts any audio file the platform's player can decode. Tildes are rejected loudly at load (`TildeInSoundFile`) for consistency with the top-level `workspace_folders` rule — agent-mux does not expand tildes in config paths anywhere; the player binaries don't expand them either, so a loud rejection beats a silent failure at the first notification. When set, agent-mux fires the OS notification *silently* and spawns a side-process player against the file: `afplay` on macOS (handles mp3/wav/aiff/m4a out of the box), `ffplay -nodisp -autoexit` then `paplay` on Linux (the broader-codec player runs first; the per-platform list is `candidate_players`). `sound_file` overrides `sound = true` — the file plays alone, no doubled-up default chime. A new `agent-mux notify-test` subcommand fires one notification matching the current config end-to-end (backend pick, sound choice, dispatcher reachability), printing a one-line confirmation of what was sent so silent failures are visible without log-spelunking. The notifier exposes `test_payload(title, host, project, blocking, message) → Payload` and `dispatch_test(payload)` so the subcommand can introspect and dispatch without going through the per-session suppression bookkeeping. A `--blocking` flag (added 2026-07-08 alongside the content overhaul) previews the sticky "needs your input" variant — flips the title state suffix, feeds a sample prompt as the body, and raises Linux urgency to `Critical` — since a real permission prompt is otherwise only observable by triggering one. Tests pin payload propagation, the per-platform player order, and the macOS `osascript` clause being omitted when `sound_file` is set (avoiding the double-up).
- ✓ notification content overhaul (2026-07-08 — user feedback that the toast was "a dense couple lines that doesn't tell you much"): reworks `Payload` formatting behind a single testable `format_notification(name, host, project, blocking, message)`. **Title** drops the redundant `agent-mux:` prefix (`notify-rust` already sets the app name) and leads with the session name + a state suffix reusing the blocked-vs-done distinction the sidebar already draws: `<name> — needs your input` for a hook-confirmed blocking prompt (permission / elicitation), `<name> — finished` otherwise. **Body** prefers the Claude Code `Notification` hook's `message` field — the actual prompt text ("Claude needs your permission to use Bash", the elicitation question) — which was already sitting in the marker's `raw_json` and is now threaded `HookEvent.message → WatcherEvent::Hook → main.rs Hook handler → Transition.message → Payload.body`; the heuristic path (no hook) falls back to the project *basename* (not the full path), and the host is appended only when remote (`local` is dropped as noise). Long messages collapse whitespace to one line and clip at 140 chars (`clip_message`). **Urgency**: on Linux a blocking prompt goes out as `notify_rust::Urgency::Critical` (sticky until dismissed — the agent is stuck until answered) via a `cfg`-gated `apply_urgency`; macOS/WSL have no urgency knob and ignore it. `blocking` is read from the session's existing `blocking_prompt` flag at the notifier call site, so both the title wording and urgency reflect current state with no extra plumbing. Tests pin the title/body/host-suffix formatting for local-finished / blocking-with-message / remote-message, the whitespace-collapse + clip, the `message` parse out of the hook payload, and the blocking→urgency flag reaching the payload. `#notifications #hooks`
- ✓ fix(remote): `SshHost::connect` verifies the master actually established (2026-05-27 dogfood): the pre-fix code spawned `ssh -fN -M -S <sock> …` and treated `status.success()` as proof the master was up — but `-fN` returns 0 the moment the parent forks the backgrounded master, *not* once the backgrounded process has opened the control socket. A ProxyCommand that doesn't survive the fork-to-background (some `coder ssh --stdio` setups, some jumphosts) made `connect` return `Ok` while no socket was ever listening, after which every `ssh -S <missing-sock>` invocation transparently fell back to a fresh connection — multiplexing was silently broken on those hosts and every poll tick (3 s transcript + 3 s pane) and session switch paid full proxy + handshake cost, exactly the failure the load-bearing "session switching never blocks on I/O" property exists to prevent. Surfaced 2026-05-27 against `gizmo.coder` (Coder proxy): `$TMPDIR/agent-mux-ssh-*.sock` was empty, no `ssh -fN -M …` process was running, but agent-mux was happily spawning `ssh -S <missing-sock> gizmo.coder tmux list-panes …` every 3 s with the matching PID. `SshHost::connect` now follows the spawn with `ssh -S <control_path> -O check <target>` and returns `Err` when the check fails; the existing startup `connect_errors` footer line surfaces the failure to the user instead of the silent degraded mode. Best-effort `ssh -O exit` cleanup runs before the Err returns so a partially-established master doesn't leak. Pinned by extending the lifecycle test to assert the connect→check→drop sequence with matching socket paths, plus a new `ssh_host_connect_fails_when_master_does_not_establish_after_spawn` regression test backed by a smarter mock-`ssh` that exits non-zero on `-O check`. Adjacent follow-up — a per-host `multiplex = false` opt-out for proxies that can't sustain `-fN -M` at all — stays in TODO since the right call there is informed by what the loud-failure surface tells us in the wild.
- ✓ fix(sidebar): Tools group is reachable again (2026-05-27 dogfood): `k`-ing up to a `DisplayRow::ToolRow` landed selection correctly, but within milliseconds the cursor snapped back into the favorites/sessions list — the Tools sidebar group was effectively unreachable on any non-empty catalog. Root cause was the re-seat plumbing rather than j/k navigation: `App::drain_updates` calls `reseat_selection_to(selected_anchor_for_reseat())` at the end of every drain (which fires on every watcher tick — attention updates, new transcripts, live-pane polls land here constantly), and `SelectionAnchor` was a struct keyed on `(SessionId, in_favorites)` with no way to represent a tool-row selection. `selected_anchor_for_reseat` fell through to `_ => None` for `ToolRow`, then `pick_reseat_target(None)` returned `first_session_index` — the first selectable session, i.e. the first favorite when the favorites group is non-empty. `SelectionAnchor` is now an enum with `Session { id, in_favorites }` and `Tool { tmux_session }` variants; the `Tool` arm anchors on the launch's `tmux_session` name (stable across removals) rather than its positional index in `ToolLaunchRegistry` (would shift when an earlier sibling was pruned and silently jump the cursor to a different tool). `pick_reseat_target` gains a `Tool` resolution branch — same-`tmux_session` match first, otherwise fall back to `first_session_index` (the Tools group is transient, no analogous "either kind" tier). Pinned by three new regression tests in the `pick_reseat_target_*` module: cursor stays on a tool row across a re-seat, tool lookup follows the tmux session name when a sibling was removed, and the cursor drops back into sessions when the anchored tool itself is pruned.
- ✓ fix(attachment): confirmation gate before spawning a parallel `claude --resume` (2026-05-27 dogfood): closes the bug where opening agent-mux on a Claude conversation already running outside agent-mux (bare terminal, no tmux) and pressing Enter on its sidebar row silently spawned a *second* Claude via `tmux_resume_argv`, leaving two processes attached to the same transcript file. Root cause: `find_pane_local` couldn't see the original Claude (no tmux server to list), and the fallback fired the resume without a check for "is anything else currently writing to this transcript." `App::attach_selected` now runs a pre-attach probe — `lsof -t -- <transcript_path>` — when the host is local and `find_pane_local` returned `NotFound`. A non-empty result arms a `AttachConfirmState { session_id, project_dir }` on App and refuses the attach; the footer paints a `⚠ live claude detected for <project>  ·  ⏎ spawn parallel resume anyway  ·  any other key cancels` banner above the normal status line. A second Enter on the same session bypasses the gate (the `already_armed` check in `should_gate_attach`); any other keystroke clears the armed state without firing the attach. Pure helpers split for testability: `interpret_lsof_output` (parser) covers the whitespace-only / non-UTF-8 / multi-pid edge cases without spawning `lsof`; `should_gate_attach` (4-bool truth table) covers the five interesting input combinations (all-trip, remote, pane-present, no-writer, already-armed); two footer-precedence tests pin "banner outranks status" and "creating still outranks banner." Scope is local-only for the first cut — the same race exists for remote sessions but the probe would need `lsof` on the remote and the dogfood signal is local-only. `find_pane_local` is now `pub` so the App-level gate can share the same lookup the driver uses.
- ✓ subagent transcripts no longer surface as flapping rows (2026-05-27 dogfood): when a session spawned a sidechain investigation via the `Task` tool, Claude Code wrote that subagent's transcript at `<bucket>/<parent-session-id>/subagents/agent-<id>.jsonl` — three levels below the projects root rather than the standard two. Startup discovery already skipped these because `Host::list_transcripts` reads exactly two levels via `read_dir`, but the live recursive `notify` watch picked them up: each subagent appeared as a brand-new dashboard row that fired Attention transitions on every internal write, generating noise for work the user hadn't asked about. The watcher's notify thread now applies the same depth-2 shape check (`is_top_level_transcript`) before any `NewTranscript` emission, so the two discovery paths filter identically. Pure-helper unit tests pin the accept (`<root>/<bucket>/file.jsonl`) and reject (subagent path, root-level loose file, path outside the root) cases. Content-marker hedge (`"isSidechain": true` on every JSONL entry) is filed in TODO against a future Claude Code layout change.
- ✓ stillborn transcripts no longer clutter the catalog (2026-05-29 dogfood): running `/clear` inside Claude Code immediately creates a fresh transcript file (a `file-history-snapshot`, the `<local-command-caveat>` envelope, the `<command-name>/clear</command-name>` envelope, and a `system` entry — verified against the actual on-disk shape) but the user often doesn't return to type a real prompt. The abandoned transcript then sat in the dashboard for 30 days (until `DISCOVERY_MAX_AGE`) titled only by its session-id hash because none of the title-deriving signals (`task.toml`, `aiTitle`, first real user message) had fired. `assemble_session` now returns `None` when all three signals are absent, applied uniformly to both startup discovery (`discover_with_cutoff`) and live discovery (`build_session` from `NewTranscript`); the watcher's existing "retry on next event when the file gains content" loop naturally surfaces the row the moment the user does type. Mirrors the subagent path-shape filter's discipline — filter at the discovery boundary, never clean up after. agent-mux-spawned worktrees still surface immediately because their `.agent-mux/task.toml` task name is itself a user-intent signal. Four new tests pin the behaviour: the realistic post-`/clear` shape is filtered, a cwd-only no-signal transcript is filtered, a task.toml-bearing session with no user message survives, and the `build_session` live-discovery path applies the same filter (so a mid-run `/clear` doesn't leak a row that startup would have hidden).
- ✓ fix(embedded-pty): EmbeddedPty drop no longer terminates the inner tmux session (2026-05-29 dogfood, exposed by the t-in-tools ship below). Repro: press `t` on a project row (terminal session "N" opens in the embedded pane), Ctrl-a Esc, Enter on a different session row (Claude attach replaces the terminal embed), Ctrl-a Esc, Enter on the terminal's tools-group row. The re-attach showed garbage briefly then crashed and removed the row; `MUX_DEBUG=1` instrumentation pinned the cause to session "N" already being gone before the re-attach probe ran, and a 200ms-interval watcher pinned the death to within ~27ms of the *first* embed swap (terminal → Claude). Root cause traced to `portable_pty 0.9`'s `UnixMasterWriter::drop` (src/unix.rs:393): it reads the master's termios `VEOF` byte (default `^D`) and writes `\n` followed by that byte to the master before closing the writer's fd, intending to cleanly close stdin for a generic child. agent-mux hosts `tmux attach` clients in the PTY — tmux attach faithfully forwards stdin bytes to the inner session's shell, which interprets `^D` as EOF and exits, closing the window, the session, and (if it was the last session) the tmux server. Bug is dormant for `[[tools]]` launches whose child is e.g. lazygit (lazygit doesn't exit on `^D`) and for the `tmux new-session -A -s agent-mux-<sid> claude --resume` shape (claude doesn't exit on `^D`), which is why dogfooding hadn't surfaced it before the t-in-tools ship made `/bin/zsh` the embed-hosted process for `t`. Fix: `EmbeddedPty` now stores a `ChildKiller` cloned from the child at spawn time (the child itself stays in the reader thread, blocked on `wait`), and `EmbeddedPty::drop` SIGHUPs the child first then sleeps 30ms to let SIGHUP propagate. `tmux attach` handles SIGHUP by detaching cleanly, closing the slave fd — so by the time `UnixMasterWriter::drop`'s `write_all(&[b'\n', VEOF])` runs, the master writes into a closed pipe and is a no-op. Pinned by `drop_preserves_tmux_session_when_hosting_an_attach_client` in `embedded_pty::tests`, which spawns a sandboxed tmux server on a `-L <socket>` (so it can't touch the user's default server), hosts `tmux attach` in an EmbeddedPty, drops it, and asserts `has-session` still succeeds. The test cleans up its sandbox server before the assert so a failure doesn't leave socket artifacts. Trade-off: every embed swap now eats a ~30ms sleep on drop — measurable but well below the "indistinguishable from a local tmux window switch" target.
- ✓ `t` terminal launches persist in the Tools sidebar group (2026-05-29 dogfood): closes the asymmetry where `[[tools]]` launches surfaced as re-attachable `⚒ name · project · [host] · Xs ago` rows but the built-in `t` terminal didn't — the only way back to a terminal was to navigate to the source session row and press `t` again, which (before this ship) refocused via a same-key synthetic SessionId. The fix lifts `spawn_terminal_local` / `spawn_terminal_remote_embed` onto the same recipe `spawn_tool_*_embed` uses: wrap `$SHELL` in a detached tmux session (`tmux new-session -d -P -F '#{session_name}' -c <cwd> <shell>` locally; the remote variant passes `exec "$SHELL"` as the trailing command so the remote tmux's `/bin/sh -c` expands the env var against the *remote* user), then attach via the embedded pane. `App::spawn_terminal_selected` pushes a `ToolLaunch { name: "terminal", … }` into `ToolLaunchRegistry` so the existing tools-group renderer surfaces the launch, and the existing prune-on-PTY-exit path (`tool_tmux_session` on the embedded struct) handles cleanup symmetrically. Design calls resolved in the same ship: (a) **name collision** — `"terminal"` (case-insensitive) joins `RESERVED_TOOL_NAMES`, so a `[[tools]] name = "terminal"` config entry now fails at load with `ConfigError::ReservedToolName` rather than rendering two indistinguishable sidebar rows; (b) **multi-launch semantics** — every `t` press creates a new tools row (matching `[[tools]]`), implemented by keying the embedded synthetic SessionId on the tmux session name (`agent-mux-terminal:<tmux_session>`) so `install_embedded`'s same-id refocus fast-path doesn't orphan the newly-spawned tmux session; (c) **outside-tmux `SuspendAndRun` path** — left alone, since the embedded path is the dogfooded default and the legacy SuspendAndRun route doesn't surface in the tools group anyway. Pinned by `pty_driver_spawn_terminal_remote_invokes_detached_tmux_new_session` (asserts the ssh-wrapped tmux argv shape for the spawn call) and `load_from_rejects_tool_name_terminal` + `load_from_rejects_tool_name_terminal_case_insensitive` (pin the `[[tools]]` validator). The local-path full-end-to-end test is deliberately not unit-tested for the same reason `spawn_tool_local_embed` isn't — it actually shells out to tmux; the shared `tmux_new_detached_tool_argv` helper has its own tests, and dogfooding covers the assembly.
- ✓ fix(remote): dead `ControlMaster` auto-reconnect (2026-06-23 dogfood): closes the "mux gets slow connecting to remotes after the laptop's been asleep overnight" complaint. The SSH `ControlMaster` was opened once in `SshHost::connect` and never health-checked afterward; when the laptop slept past `ControlPersist=600` (10 min) and the TCP connection dropped, every later `ssh -S <socket>` silently fell back to a full TCP+TLS+auth handshake — so each 3 s poll tick *and* every session switch paid proxy + handshake cost, the exact "session switching never blocks on I/O" violation the 2026-05-27 verify-after-spawn fix existed to prevent at connect time, now recurring mid-run. New `Host::ensure_connected() -> io::Result<bool>` (trait default `Ok(false)` for `LocalHost`; `SshHost` overrides): a cheap `ssh -O check` liveness probe (local-socket round-trip, no network) and, when it shows the master dead, a stale-socket cleanup + re-spawn of the same `-fN -M` master `connect` uses — both paths now share a `spawn_master` helper so they issue identical flags and the same verify-after-spawn guarantee. The transcript poller (`start_polling_host`) calls it at the top of every tick — *off* the attach hot path — so a master that died overnight is rebuilt within one `REMOTE_POLL_INTERVAL` (3 s) of the laptop waking, warm before the user switches sessions. Re-establishment is serialised by a per-host `reconnect_lock` so the transcript and pane pollers sharing one `Arc<SshHost>` can't both delete the socket and spawn duelling masters; a successful reconnect logs `re-established connection to host '<id>'` to stderr, a failed one logs and retries next tick (treated as "still down," never fatal to the poller). Pinned by four tests: alive-master probe is a no-op (one `-O check`, no respawn), dead-master probe triggers the `-fN -M` respawn, a never-reestablishing host surfaces `Err` after attempting the respawn, and `LocalHost::ensure_connected` is a no-op. Deliberately *not* called synchronously on the attach path (that would be UI-thread I/O); the proactive poller is the warm-keeping mechanism. Follow-ups in TODO: a footer "reconnecting to <host>…" hint, exponential backoff for a long-down host, and whether the pane poller should drive reconnect too. `#remote #ssh #reliability`
- ✓ fix(worktree): `.agent-mux/task.toml` stays out of git (2026-06-23 dogfood): the per-session task metadata agent-mux writes into a worktree (`.agent-mux/task.toml`) was an untracked file with nothing stopping `git add .` — the user's hand, or an agent running git on their behalf — from sweeping it into the repo, which then had to be manually un-committed. `WorktreeManager` now drops a `.agent-mux/.gitignore` containing `*` alongside the metadata at create time, so the whole directory (the gitignore included) is invisible to `git add` / `git add -A`. Chosen over appending to the worktree's `.git/info/exclude` because the in-dir gitignore needs no read-modify-write of a file shared across a repo's worktrees (no race on concurrent creates) and rides the same `Host::write_file` path as the metadata, working identically on local and remote worktrees; the existing `rm -rf .agent-mux` pre-clean on `git worktree remove` sweeps it away with the rest. Only newly-created worktrees get the gitignore — pre-existing ones keep their untracked `task.toml`, which the remove-path pre-clean still covers (its comment now notes the back-compat reason). Pinned by `create_keeps_task_metadata_out_of_git`, which creates a real worktree, writes a sibling user file, and asserts `git status --porcelain` surfaces the user file but nothing under `.agent-mux/`. `#worktree #git`
- ✓ favorited sessions no longer vanish before their host connects (2026-06-23 dogfood): a favorite rendered only when its live `Session` was in the catalog, so a favorited *remote* session blinked out at startup the moment `reconcile_host` dropped the disk-cache seed and the live discovery hadn't — or couldn't (recency-filtered, or host down) — re-proved it; the user returning in the morning saw their pins disappear until each remote reconnected. The `FavoritesStore` now carries cached per-entry metadata (`FavoriteMeta`: last-seen title + project dir, `#[serde(default)]` so older keys-only files still load), refreshed from the live session on every catalog drain. The sidebar builds `FavoritePlaceholderRow`s from that metadata for favorites with no live catalog entry — dimmed "unconfirmed" rows (cached title, unknown glyph, `⋯` for activity, `[host]`) appended after the live favorites — so a pin stays visible whether its host is mid-connect or offline entirely. When discovery surfaces the real session the placeholder is replaced by the live `FavoriteSessionRow` and the cursor follows it onto the confirmed row (new `SelectionAnchor::FavoritePlaceholder`, host-*and*-id disambiguated so the same id on two hosts never cross-matches). Placeholders are selectable: `f` dismisses one (resolving `(host, id)` from the row, so a permanently-gone favorite is one keystroke from removed rather than a `favorites.json` edit), and Enter surfaces a `waiting for host <h>` status instead of a silent no-op. The catalog stays strictly "discovered sessions" — placeholders live entirely in the render/selection layer, driven by the store, so no catalog consumer (attach, attention, pollers) ever sees a synthetic session. Pinned by dashboard tests (placeholders append after live favorites; header shows when only placeholders exist; empty case stays headerless), re-seat tests (cursor stays on a placeholder, follows it onto the live row when it arrives, host-disambiguates a shared id), favorites-store tests (metadata round-trip, keys-only forward-compat, `record_meta` change-only), and a search-predicate test. `#favorites #remote #ui`
- ✓ blocked-on-a-prompt vs done, distinguished in the sidebar (2026-06-23 dogfood): `NeedsInput` collapsed two states the user reads differently — "the agent is waiting on my answer to a permission/elicitation prompt" vs "the agent finished its turn." The transcript heuristic can't tell them apart (both are an assistant entry with `stop_reason != "tool_use"`), but the `Notification` hook can: the ingest now classifies `notification_type` (`is_blocking_prompt` → true for `permission_prompt` / `elicitation_dialog`, false for `idle_prompt` / unlabelled) and threads it through `HookEvent` → `WatcherEvent::Hook` → `SessionCatalog::apply_hook_event` into a new per-session `blocking_prompt` flag. `apply_heuristic_attention` clears the flag whenever it re-applies (a transcript-derived state is never a blocking prompt), so the marker can't stick on past the user's answer. The sidebar renders a distinct `◆` glyph when `blocking_prompt && effective_attention == NeedsInput` (reusing the `needs_input` colour), so `◆` ("answer me") reads apart from `●` ("done") at a glance. Deliberately a `bool` sub-classification of `NeedsInput` rather than a new `Attention` variant: the notifier's `NeedsInput`-keyed suppression, the idle overlay, and the disk cache all stay untouched, and the notification fires identically for both (the distinction is display-only, which is what the user asked for). Gated on the hook being installed — without it everything degrades to today's `●`. Pinned by catalog tests (blocking hook sets the flag + heuristic clears it; idle nudge leaves it false) and hook-ingest tests (`is_blocking_prompt` allowlist; `parse_marker_content` derives the flag from `notification_type`). Follow-up filed: a dedicated `[theme.blocked]` colour (today it shares `needs_input`'s). `#attention #hooks #ui`
- ✓ taller, two-line session rows (2026-06-24 dogfood): the sidebar's single-line rows were judged "too simple." Each session now renders as a two-line `ListItem` — line 1 glyph + title (as before, minus the inline age/host), line 2 a dim status line: `<state> <time-in-state> · running <total>` (e.g. `working 18m · running 45m`, `blocked 2m`, `done 5m · running 3h`). State words: `blocked`/`done`/`working`/`idle`/`—` via `attention_word`. The row stays one `DisplayRow`, so navigation, selection, re-seat, and the favorites/placeholder machinery are all unaffected — only the `ListItem` becomes multi-line (the whole block highlights). Two new `Session` fields back the durations: `attention_entered_at` (stamped by the catalog on every attention transition — `update_attention`/`apply_heuristic_attention`/`apply_hook_event` — and initialised to the transcript mtime at discovery, a good proxy for stopped states; not cached, reseeded from `last_activity` on cache load) drives "time in state"; `started_at` (the `.agent-mux/task.toml` `created_at`, extracted in `assemble_session`; cached as `started_at_secs` with `#[serde(default)]`) drives "running <total>". `started_at` is `None` for externally-started sessions (no task.toml), so they show just `<state> <time-in-state>` and omit the total — deriving a start from the first transcript entry's timestamp is filed as a follow-up (it needs a date parser). `compact_elapsed` formats both durations without the "ago" suffix; a future timestamp (clock skew) drops the cell rather than printing a bogus value. Layout chosen with the user from ASCII mockups ("rich status line" + "both durations"). Pinned by `attention_word`, `compact_elapsed`, and four `format_session_row` tests (two-line structure, state+durations present, `◆`+`blocked` for a blocking prompt, total omitted when no start time). The standalone "Xm ago" age cell is gone — time-in-state subsumes it for stopped states and is the more useful number for working ones. `#ui #sidebar #attention`
- ✓ per-project row cap (2026-06-24 dogfood): the two-line rows above doubled each session's height, so a busy project could flood the sidebar. Each project in the natural host → project tree now shows at most `[ui] sessions_per_project` of its most-recent sessions (default 5, `0` = no cap), with the remainder collapsed behind a dim non-selectable `+ K more` row (`DisplayRow::ProjectOverflow(hidden)`). The cap is **lifted while a search filter is active** — narrowing should surface every match, so search doubles as the "show everything" escape hatch and no extra selection surface is needed (the overflow row isn't selectable; hidden older sessions are reached via search or by favoriting, and favorites are pinned at the top regardless). Implemented as a `cap: Option<usize>` parameter threaded into `build_display_rows_filtered` (the per-project loop truncates the recency-sorted slice and emits the overflow row); `current_rows` computes it from `config.ui.sessions_per_project`, normalising `0`→`None` and forcing `None` while searching. New `[ui]` config section (`UiConfig`, `#[serde(default)]`, `DEFAULT_SESSIONS_PER_PROJECT = 5`), surfaced in `agent-mux help`'s reference and `agent-mux config`'s live-parsed status. Pinned by dashboard tests (truncate-to-most-recent + `ProjectOverflow(K)`; `None` shows all; cap ≥ count emits no overflow) and config tests (default constant; `[ui]` parse). `#ui #sidebar #config`
- ✓ sidebar readability pass (2026-06-24 dogfood, on the two-line rows above): four fixes from looking at the real dashboard. (1) **Dropped the per-row attention glyph** — line 1 is now title-only; the attention signal moved to the line-2 state *word*, which is bold + themed `attention_color` when the session wants the user (`blocked`/`done`) and dim otherwise (`working`/`idle`). The word reads on any theme (no glyph legend to learn) and the weight gives an at-a-glance cue even on the uncoloured default preset; `attention_glyph`/`BLOCKED_GLYPH` are gone. (2) **Flush-left session blocks** — titles now sit at column 0 (was indent 4 *plus* a glyph column), with the status line nested two columns under each title; the `──host──` / dim-project headers carry the grouping and the list's selection bar shrank from `▌ ` to a 1-column `▌`, so titles get the full sidebar width instead of being pushed halfway across and clipping. (3) **Readable selection** — the highlight is now a dark-grey background (`Color::Indexed(238)`) instead of `REVERSED`, which had inverted the dim detail line into low-contrast dark-on-light. (4) **Relabelled the total** from `running <n>` to `<n> old` — it's the worktree's creation age (task.toml `created_at`), so "running 27d" on a 27-day-old worktree read as a bug rather than "this worktree is old." The placeholder row and `+ K more` overflow followed the same indent/glyph changes. Tests updated (line 1 carries no glyph; `blocked` not `done`; `<n> old` not `running`). README/ARCHITECTURE updated to match. `#ui #sidebar #dogfood`
- ✓ fix(attention): blocked-prompt detection no longer self-clobbers; oversized-final-message recovery (2026-06-24 dogfood): two transcript-detection gaps surfaced by dogfooding ("blocked detection is a little off"; "not always detecting that something completed"). (1) **Blocked clobber.** A permission prompt's transcript signature is an assistant entry with `stop_reason == "tool_use"` — the heuristic reads that as `working`, while the `Notification` hook (correctly) pins it as `blocked`. The hook pin is only protected while `transcript_mtime ≤ hook_timestamp`, but the triggering `tool_use` write and the hook fire milliseconds apart with no guaranteed order, so the `tool_use` entry's mtime routinely edged *past* the pin — `apply_heuristic_attention` then cleared the pin and the `blocking_prompt` flag, flickering the row back to `working` while the agent was actually waiting on the user. Fix: the heuristic now reports a `from_tool_use` discriminator (new `watcher::AttentionDerivation` + `derive_attention_detail{,_from_content}`, threaded through `AttentionUpdate` from all five producer sites into `apply_heuristic_attention`). A bare `tool_use` update — the prompt's *own* entry — never releases the pin or clears the flag, regardless of mtime; only genuine progress (a `tool_result` / user message / turn-end, `from_tool_use == false`) with a strictly-newer mtime hands authority back to the heuristic, which is exactly the user-answered-the-prompt path. (2) **Oversized final message.** `derive_attention` reads only the last `TAIL_BYTES` (32 KiB); a final assistant message larger than that leaves no complete JSON line in the window, so a *completed* turn derived as `Unknown`. `derive_attention_detail` now escalates to a 1 MiB re-read once when the tail yields `Unknown`, recovering the real `done`. Pinned by catalog regression tests (`bare_tool_use_does_not_clear_blocking_pin_even_when_mtime_races_past`, `genuine_progress_after_block_clears_flag_and_pin`) and watcher tests (`detail_flags_from_tool_use_only_for_trailing_tool_use`, `derive_attention_recovers_done_from_oversized_final_message`). Note: the installed hook command may point at a stale binary path (e.g. a pinned nix-store build that predates this feature) — re-run `agent-mux install-hooks` from the current build to keep it current. `#attention #hooks #dogfood`
- ✓ status icons + done/blocked colour split + theme expansion (2026-06-24 dogfood, surfaced comparing against Herdr's colour-circle sidebar): three intertwined changes to the line-2 state cell. (1) **Re-added status icons.** The 2026-06-24 readability pass had dropped the per-row glyphs in favour of a bare state word; dogfooding found the all-text line harder to scan than a coloured icon. The state line now leads with a coloured glyph — `✓ done`, `! blocked`, `◐ working`, `○ idle`, `· unknown` (new `attention_glyph` helper) — *keeping* the word beside it, so the prior "legend to learn / must read on mono" objections still hold (glyph = at-a-glance, word = fallback). Distinct *shapes* (✓ vs !) carry the blocked/done split even for a colourblind eye or the `mono` preset. (2) **done vs blocked colour split.** Both states are `NeedsInput` and shared the `needs_input` colour (red) — "answer me" and "finished" looked identical apart from the word. New `[theme] blocked` element (resolved via `Theme::blocked_color`, falling back to `needs_input` when a theme only colours one) lets blocked render red while done renders green; all 8 presets recoloured to green-done / red-blocked using each palette's authentic green, and the **`default` preset now ships colours** (green/red/amber/dim) so the icons read out-of-box with no config — `mono` remains the fully-uncoloured escape hatch. (3) **`[theme]` schema expansion** (the broader "expand what theming covers" ask): two structural colours un-hardcoded — `focus_border` (was a fixed `Color::Cyan` in `focus_border_style`) and `selection` (was a fixed `Color::Indexed(238)` row-highlight background), each resolved with its historical value as the fallback so existing configs are unchanged and neither appears in the attention-only `agent-mux themes` table. `ThemeConfig`/`Theme` gain `blocked`/`focus_border`/`selection`; `from_config` overlays all three; the table gained a `! blocked` column and updated glyphs; `agent-mux help` and `agent-mux config` list the new keys. Pinned by config tests (default-now-coloured, green/red split per preset, `blocked_color` fallback, structural-field parse), a `themes` table test (new `blocked` column), and main.rs render tests (`attention_glyph` mapping, `! ` blocked icon, done-green-vs-blocked-red span colours, themed `focus_border`). Deferred to TODO: per-element backgrounds and `[theme.header]` bold/fg. `#ui #sidebar #theme #dogfood`
- ✓ themed frame background + embedded-terminal harmonisation (2026-06-24, from the theme-expansion discussion): a `[theme] background` element paints a colour behind the whole agent-mux frame — sidebar, header, footer, *and* the gaps around the embedded terminal — so a themed dashboard reads as one surface instead of floating on the raw terminal background. `draw` fills `frame.area()` with the colour first (fg-only widgets show it through); `draw_embedded` passes it as the `tui-term` `PseudoTerminal`'s base style (new pure `embedded_base_style` helper) so the embedded session's *terminal-default* cells match the sidebar. The coloured presets (`bright`/`warm`/`cool`/`solarized`/`gruvbox`/`nord`) ship a subtle dark `background` (each its palette's canonical dark base — e.g. Solarized `base03`, Nord polar-night, gruvbox `dark0_hard`); `default` and `mono` leave it unset so agent-mux still composes with the user's own terminal background by default. **Deliberately render-layer only** — agent-mux does *not* set tmux `window-style`/`pane-style` on the session: those persist past the client (the user would detach and find their session recoloured), clobber an existing tmux theme, and mutate the remote tmux server over SSH, all of which contradicts the SPEC's "runs on top of tmux, does not configure it." The trade-off is that a program painting its own background (Claude Code's TUI) keeps it, so the harmonisation shows in the margins/default cells rather than as a full fill — the honest ceiling of a non-intrusive approach. `background` is structural (config-only, not in the `agent-mux themes` accent table) and clearable per-preset with `background = ""` (e.g. to run Solarized accents over a light terminal). Pinned by config tests (coloured presets carry a bg, `default`/`mono` don't, override + empty-string-clear) and a render-helper test (`embedded_base_style` carries the themed bg or stays empty). The fuller-fill alternative (mutating tmux) and per-state background chips / `[theme.header]` stay deferred in TODO. `#ui #theme #embedded-pty`
- ✓ distinct sidebar panel via `[theme] sidebar_bg` (2026-06-24, follow-up to the frame background above): dogfooding the uniform frame background showed it's nearly invisible — a full-screen tint has nothing to contrast against, and the presets' near-black bases (gruvbox `dark0_hard` etc.) vanish on a dark terminal. The fix is *structure*, not a louder tint: the sidebar is now painted a shade **above** the frame `background` so it reads as a panel against the darker content/terminal area (the Herdr look). New `[theme] sidebar_bg` element + `Theme::sidebar_bg_color` (falls back to `background`, so a theme that only sets `background` still gets a uniform frame); `draw` sets it as the sidebar `Block`'s style. The coloured presets ship a `background`/`sidebar_bg` pair drawn from each palette's bg0/bg1 (gruvbox `#1d2021`/`#3c3836`, Nord `#2e3440`/`#3b4252`, Solarized base03/base02, …); `default`/`mono` leave both unset. Pinned by config tests (presets pair a distinct sidebar shade, `sidebar_bg_color` fallback chain) and the existing render-fill test. `#ui #theme #sidebar`
- ✓ fix(remote): reconnect/poll backoff for an unsustainable master (2026-06-24 dogfood): dogfooding surfaced a burst of short-lived ssh-running tmux windows ("no output, gone in 5–10 s") on a Coder-proxied remote whose `ControlMaster` can't stay up. Root cause: the dead-master reconnect (shipped 2026-06-23) re-spawned a doomed `ssh -fN -M` (≈5 s `ConnectTimeout`) from the transcript poller *every* 3 s tick, and the pane poller independently fired a fresh `ssh tmux list-panes` on the dead socket each tick — overlapping 5 s attempts on a 3 s interval pile up into a visible storm. `SshHost::ensure_connected` now carries shared backoff state (the `reconnect` mutex grew from `Mutex<()>` to `Mutex<ReconnectState>`): after a failed re-spawn it sets a wait window (6 s, doubling to a 60 s cap) and reports the host down *without* re-spawning until the window elapses; a healthy probe resets it. The pane poller now gates its tmux query behind `ensure_connected` (sharing that backoff), reporting an empty snapshot while down instead of paying the round-trip — so a down host costs one cheap `ssh -O check` per tick, not a fresh `ConnectTimeout`-bounded connection from two pollers. The cheap probe and the serialise-the-respawn lock are unchanged. Pinned by a new test (two immediate failed `ensure_connected` calls re-spawn only once) alongside the existing alive/dead/err cases. Closes the backoff + pane-poller follow-ups filed with the original reconnect. `#remote #ssh #reliability`
- ✓ quickswitcher fuzzy-jump modal (2026-06-24 dogfood, the batch's top ask): `Ctrl-P` opens a centered modal that fuzzy-finds across *every* jump target — live sessions (most-recently-active first), running tool launches, and offline favorites — and on Enter attaches to the highlight immediately. This is the "which window was that conversation in?" friction the SPEC opens with, driven to one chord + a few letters. New `quickswitcher` lib module: `QuickSwitcher` holds only a query buffer + `ListState` + a `filtered: Vec<usize>` ranking into an immutable `entries` snapshot the caller builds at open time (`App::quickswitch_entries`), so the modal never touches the catalog or a host — "switching never blocks on I/O" by construction. Matching is a hand-rolled subsequence scorer (`fuzzy_score`, no new dependency) over a per-entry `haystack` of title + project + host: each matched char scores, with a contiguous-run bonus (+6) and a word-boundary bonus (+4) so `depl` ranks `deploy-fix` above a scattered `dance-pelican`, a small gap penalty, and a mild short-haystack preference; empty query keeps the caller's recency order. Enter on a candidate yields a `SwitchTarget` the run loop maps to a `SelectionAnchor` (via `switch_target_anchor`), re-seats the sidebar cursor onto the matching row, and runs the **same** attach path a manual Enter would — the Enter dispatch was refactored into a shared `App::attach_under_cursor` so the switcher is a thin selection layer over the one attach flow, not a parallel one (a picked offline favorite re-seats onto its placeholder and surfaces "waiting for host" rather than attaching). The modal is a sibling of `modal`/`delete_modal` with the same at-most-one-overlay guard; routed through `ActionOutcome` (not a bare handler) because an immediate attach may return a `SuspendCommand` the loop must run. Navigation: `↑`/`↓` plus fzf-style `Ctrl-p`/`Ctrl-n` (so a held open-chord walks the list), `Esc` cancels, selection re-aims to the top match on each keystroke. Bound on `Ctrl-P` (the fuzzy-finder convention; no single-letter sidebar collision). Pinned by 12 module tests (subsequence match/non-match, contiguous-beats-scattered ranking, empty-query recency order, typing filters+ranks, no-match clears selection + inert Enter, Enter picks the highlighted target, Esc cancels, Ctrl-n/p navigate without typing, wrap-around, backspace widens). `#ui #navigation #dogfood`
- ✓ quickswitcher: attention status rows + "jump to what needs me" + open from within a session (2026-07-08 — user ask, delivers the toast-click-free half of the notification-return idea): the switcher now doubles as the fast path back to a session that pinged, reusing the sidebar's own vocabulary. Each session row carries a status prefix — the sidebar's `attention_glyph` + `attention_word` (`! blocked`, `✓ done`, `◐ working`, `○ idle`) with the same themed colour and bold-for-needs-user — via a new `switch_status_for(session, theme) → (rank, SwitchStatus)` that funnels through the identical `effective_attention` / `attention_color` / `blocked_color` lookups the dashboard uses (so the switcher can't drift from the sidebar). Tools and offline placeholders have no attention and render a blank, width-matched status column so labels still align. The empty-query view is **attention-sorted**: a stable sort by rank floats `blocked` (0) then `done` (1) above everything else (2 — working/idle sessions, tool launches, offline pins), each group keeping its prior order (recency for sessions); the moment the user types, `fuzzy_score` re-ranks as before, so the sort only shapes the landing view. `SwitchEntry` gained an `Option<SwitchStatus>` (glyph/word/colour/bold as plain data, keeping `quickswitcher` decoupled from the `Theme` type). **Opening from within a session**: a new `Ctrl-a p` leader chord (sibling of `Ctrl-a Esc`) opens the switcher over the live embedded session — the `leader_chord_transition` state machine gained an `OpenQuickSwitcher` arm matching plain `p` while armed (`Ctrl-a Ctrl-p` still passes through to inner tmux as prefix+p). Focus moves to the sidebar so the overlay receives keys while the PTY stays alive underneath; the origin is remembered as `App.quickswitch_return_focus: Option<Focus>` so **Esc drops the user back into the session** they were in rather than the sidebar, while **Enter attaches** the picked one (the existing `attach_under_cursor` sets terminal focus itself). Pinned by a `TestBackend` render test (status glyph+word+label paint) and unit tests for the two new leader-chord cases (plain `p` opens, `Ctrl-p` still forwards) and `switch_status_for`'s rank + vocabulary across blocked/done/working. `#ui #navigation`
- ✓ fix(remote): background ssh subprocesses no longer inherit `$TMUX` (2026-06-24 dogfood): the backoff fix cut the *frequency* of the stray-ssh-window storm but not its mechanism — the user still saw short-lived ssh windows open *and steal focus* in the tmux session showing their Claude work. Root cause: every background ssh subprocess agent-mux spawns (`spawn_master`, `master_check`, the `-O exit` teardown, and the `ssh_command` builder behind all transcript-poll / discovery reads) was built with a bare `Command::new(ssh)`, inheriting the full parent environment. When agent-mux runs *inside* tmux, that includes `$TMUX` / `$TMUX_PANE` pointing at the user's active server — so the ssh client's *local* `ProxyCommand` (the Coder host's `coder ssh --stdio`) inherited it too and a tmux-aware proxy/wrapper could open windows in that server, auto-focused, gone when the ~5s connect attempt ended. New `SshHost::background_ssh_command(ssh_binary)` centralises the four spawn sites and `env_remove`s `TMUX` + `TMUX_PANE`, so neither the ssh client nor its proxy can reach the user's tmux server. The interactive attach paths are deliberately untouched — they run through the tmux / PTY drivers (`run_tmux`, the embedded `PtyDriver`), which *must* see the real `$TMUX` to host the session — so a legitimate `Enter`-attach still lands where it should. Pinned by `background_ssh_commands_strip_tmux_env` (asserts both the shared builder and the `&self` poll/discovery builder report `TMUX`/`TMUX_PANE` as removed via `Command::get_envs`). Highest-confidence match for the symptom (windows in the *user's* server ⇒ something inherited `$TMUX`); if any persist, the next move is the env-gated spawn logger + a circuit-breaker that stops polling a host whose master can't be sustained (filed in TODO). `#remote #ssh #tmux #reliability #dogfood`
- ✓ favorites sort alphabetically, not by recency (2026-06-25 dogfood): the pinned favorites group ordered by `last_activity` desc, so rows jumped around under the user every time a session's transcript ticked — making the curated set hard to navigate precisely because it kept reshuffling. Favorites now sort alphabetically by the label each row *displays* (user rename → transcript/task title → `(id-suffix)` fallback, lowercased), so the order is stable and matches what the eye reads. `build_display_rows_filtered` gained a `fav_sort_key: Fn(usize) -> String` closure (the pure builder stays decoupled from the rename store; the `main.rs` call site supplies the key from `session_names` + `Session.title`), and `sort_by_cached_key` computes each key once and ties stably. Offline favorite *placeholders* sort the same way in `favorite_placeholders` (no rename layer — they render `meta.title` directly), so the whole group reads top-to-bottom by label. The natural host→project tree still orders by recency — only the curated favorites group changed. New `favorite_sort_key` + `short_id_suffix` helpers (the latter also de-duplicates the title-less `(last-6)` fallback the session-row and placeholder renderers each open-coded). Pinned by tests: favorites order alphabetically (recency order must NOT survive), sort by title not id when titled, and the key's rename→title→id precedence. `#ui #dogfood`
- ✓ sidebar widens on `Ctrl-a Esc` (2026-06-25 dogfood): with a session embedded, the sidebar sat at a fixed compact 40 columns even after the user escaped focus back to it to navigate — long session titles truncated exactly when the user was trying to read them to pick a row. The sidebar now widens to ~60 columns whenever focus is on it (`Focus::Sidebar`) and snaps back to compact the moment focus returns to the terminal, so the session pane dominates while typing and the list gets room while navigating. Width comes from the pure `embedded_sidebar_width(focus, total_width)`: expanded size clamped so the terminal pane keeps at least `MIN_EMBED_TERM_WIDTH` (30) and floored at the compact width so a very narrow screen just stays compact rather than going backwards. The existing per-frame resize cascade (`should_resize_pty` against `last_size`) carries the one PTY resize each focus toggle for free. Pinned by four tests (compact while terminal-focused, full expand on a wide screen, clamp yields a usable terminal pane mid-width, never-narrower-than-compact on a tiny screen). Widths are hardcoded for now; a `[ui]` knob is filed in TODO alongside the existing compact-width config candidate. `#ui #embedded-pty #dogfood`
- ✓ fix(attachment): returning to a session no longer re-opens a launched tool (2026-06-30 dogfood): with a session embedded, launching a `[[tools]]` command (terminal / lazygit / vim) and then pressing Enter on the session's sidebar row kept bringing up the *tool* instead of Claude. Root cause: a tool launch runs in a detached tmux session spawned with `-c <project_dir>`, so its pane's `pane_current_path` is identical to the parent session's. When the session row lacks a deterministic `agent-mux-<id>` tmux session — externally-started sessions, which dominate the remote "attach to existing" case, hence the user seeing it on a remote — `resolve_pane_target`'s stage-1 pin misses and the stage-2 cwd fallback returns whichever cwd-matching pane lists first, which can be the tool's. `install_embedded` then pinned the embed's `session_id` to the Claude row while its argv pointed at the tool, so every later Enter hit the same-id refocus fast path and just re-showed the tool. Fix: the attach path now passes the live tool-launch tmux session names (from `ToolLaunchRegistry`, filtered to the session's host) down through `attach` → `find_pane_{local,remote}` → `resolve_pane_target`, which skips those sessions in the cwd fallback — the `agent-mux-<id>` stage-1 pin is untouched since a tmux-auto-named tool session can never collide with it. When the only cwd match is a tool, the resolver returns `NotFound` and the caller falls through to the resume path, the correct behaviour for a session with no live Claude pane. Pinned by three `resolve_pane_target` tests (tool pane sharing the cwd is skipped, all-excluded yields `None`, an excluded name never blocks the `agent-mux-<id>` pin). A deeper consolidation — giving externally-started rows their own pinned tmux session on first attach — stays filed in TODO. `#attachment #remote #embedded-pty #dogfood`
- ✓ feat(embedded-pty): a failed attach lingers in the pane instead of flashing away (2026-07-02 dogfood): when the spawned `ssh … tmux attach` (or the `tmux new-session … claude --resume` resume path) died *immediately* — a non-zero exit within 750 ms of spawn — the embedded pane used to flash and vanish back to the sidebar with the error unread, giving no clue why the session wouldn't open. The motivating case: a remote host whose tmux **server** was stale after a dotfiles tmux-binary upgrade rejected every new client with `open terminal failed: not a terminal`, exit 1, in ~200 ms. Now `drain_pty_events` classifies the exit (`classify_pty_exit`): a *session* attach (not a `[[tools]]` launch) that dies fast + non-zero keeps its PTY on screen — the vt100 parser still holds the final frame, so the error stays legible — retitles the block `⚠ attach failed — Enter to retry`, and drops focus to the sidebar. Enter respawns (the `install_embedded` same-id fast path skips a failed pane), Esc-from-sidebar dismisses it. Clean or slow exits (a real session you detached from) and tool launches keep the old drop-and-return path. No footer text added — the error sits where you were already looking. Pure `classify_pty_exit` / `terminal_pane_title` are unit-tested. `#embedded-pty #attachment #dogfood`
- ✓ feat(observability) + fix(remote): diagnostics log, and background SSH no longer paints over the TUI (2026-07-02 dogfood): two related fixes to "text appears over the dashboard." (1) Background reconnect/poll chatter (`re-established connection to host …`, reconnect failures) was `eprintln!`'d from the per-host poller threads while ratatui owned the alternate screen, so it painted over the UI and — with no scrollback under the alt-screen — was unrecoverable afterward. A new `logging` module opens a process-wide sink at `~/.cache/agent-mux/agent-mux.log` (truncated per run, best-effort, never panics); the runtime messages go there, and the pre-alt-screen startup prints (notifications backend, hook-watcher fallbacks) are tee'd to both stderr and the log so there's one place to look. (2) The larger culprit: `SshHost::spawn_master` ran `ssh -fN -M` with no fd redirection, so the *persistent* backgrounded master inherited agent-mux's terminal fds for its whole life and could paint ssh warnings / `ProxyCommand` output over the alt-screen at any later moment (a reconnect after idle especially) — now nulls stdin/stdout/stderr to match its `master_check` / Drop / cleanup siblings. Every other production spawn (notifications, `.output()` reads, worktree `git`) was audited and already safe. Record format is a unit-tested pure `write_record`. `#observability #remote #ui #dogfood`
- ✓ fix(attachment): sessions sharing a directory no longer open the wrong one (2026-07-07 dogfood): the user ran several Claude sessions in one directory (multiple `N` no-worktree sessions, same repo root) and selecting a row kept attaching to a *different* session's pane. Root cause: `resolve_pane_target` pins a row to its pane by the tmux session name `agent-mux-<session-id>` (stage 1) and only falls back to matching by `pane_current_path` (stage 2) when no such name exists — but `PtyDriver::spawn_session` let tmux *auto-name* the session (`new-session -P -F '#{session_name}'`), so agent-mux-spawned sessions never had the id-derived name and every same-directory row resolved to the first cwd-matching pane. (This is the case the 2026-05-20 "per-session tmux pane resolution" entry above claimed "won't surface for agent-mux-spawned sessions" — it did, because spawned sessions weren't actually getting the name.) Fix: `spawn_session_local`/`_remote` now mint a fresh v4 UUID (read straight from `/dev/urandom` — no crate, no subprocess), name the tmux session `agent-mux-<uuid>`, and launch `claude --session-id <uuid>`. Claude Code writes its transcript as `<uuid>.jsonl`, so the `SessionId` discovery derives from the transcript stem *equals* the tmux session name — stage-1 name matching now pins every spawned session to its own pane from birth, no cwd guessing. Chosen over a first-attach rename because it's correct-by-construction (the name reflects the real conversation) and never mutates the user's existing tmux sessions. Residual (filed in TODO): two sessions started *outside* agent-mux in one directory still collide on the cwd fallback — disambiguating those needs transcript→pane process matching. New unit tests pin the argv shape and the uuid format; verified end-to-end that `claude --session-id` writes `<uuid>.jsonl`. `#embedded-pty #attachment #dogfood`
- ✓ fix(attachment): cwd fallback no longer steals a named session's pane (2026-07-08 dogfood): the same-day spawn-naming fix above only covered the case where *both* colliding sessions were post-fix agent-mux spawns — it didn't help a *mixed* pair, which is what the user actually hit next. Live state: one tmux session in the repo dir (`agent-mux-<this-conversation-id>`, correctly named) and a second sidebar row for another conversation with *no* live pane. Selecting the second row still opened the first, because `resolve_pane_target`'s stage-2 cwd fallback returned the first pane whose cwd matched — including the neighbour's `agent-mux-`-named pane. Fix: the cwd fallback now **skips any `agent-mux-`-named session**, since those are reachable only via the stage-1 exact-id match and belong to one specific row. A skipped row falls through to `NotFound` → `claude --resume`, spawning it into its own `agent-mux-<id>` session instead of hijacking the neighbour's. This also completes the original 2026-05-28 "two SessionIds collapsing onto one pane" report (named + un-named pair). No retroactive action needed — already-running un-named sessions self-heal on their next attach (the resume path names them). Two regression tests: a lone neighbour-named pane yields `None`; a neighbour-named pane plus an un-named pane resolves to the un-named one. Remaining residual (TODO): two *un-named* sessions in one cwd still can't be told apart. `#embedded-pty #attachment #dogfood`

### Multi-agent CLI support

agent-mux becomes a multiplexer for terminal agent CLIs that persist tail-parseable transcripts, with Claude Code as the reference agent and OpenAI Codex + Pi as the first additional targets. Shipped as work packages WP0–WP9 (2026-07-09/10) behind the `AgentCli` seam (`docs/plans/2026-07-09-multi-agent-cli.md`). Claude-only behaviour stays byte-identical to before throughout. **Codex/Pi caveat:** the Codex and Pi paths are tested against synthetic fixtures authored from format research (Codex rust-v0.144.1, Pi v0.80.6, researched 2026-07-09); as of 2026-07-10 they have not been verified live against real `codex`/`pi` binaries — current-release best-effort. Per-agent gaps (Codex `.jsonl.zst` cold rollouts, Codex shell-command edits, Codex blocked-on-approval needing the hook, Pi extension-added gates) are documented in the README.

- ✓ `AgentCli` trait + Claude reference impl (WP1): every agent-CLI-specific concern — transcript root + tree shape, JSONL parsing (attention, title, edited files), spawn/resume argv, session-id derivation — moved behind one trait keyed by a `Copy` `AgentKind` enum, with Claude as the sole runtime agent (pure refactor, no behaviour change).
- ✓ `[agents]` config + multi-root discovery/watch plumbing (WP2): an `[agents.<label>]` table (`enabled` — claude on, codex/pi off; `binary`; `transcript_root`) plus per-host `[hosts.<name>.agents.<label>]` overrides, with the legacy per-host `transcript_root` key kept as a claude alias. Discovery iterates (host × enabled agents); the single local `notify` watcher watches every enabled agent's root and routes events path→agent by longest-prefix match; remote pollers tick each enabled root per host. `NewTranscript`/`Attention` events carry `AgentKind`; the disk cache round-trips it. With no `[agents]` table, behaviour is byte-identical to Claude-only. Resolved accessors `Config::enabled_agents()` / `Config::transcript_root_for(host, kind)`; unknown labels rejected at load.
- ✓ codex read path — discovery + rollout parser (WP3): the depth-4 `YYYY/MM/DD/rollout-*.jsonl` transcript predicate (rejecting `.jsonl.zst` cold rollouts and wrong-depth/non-date paths); trailing-uuid id derivation (parsed from the *end* of the stem, past the dash-laden local timestamp); head parse of `session_meta` cwd + a first-user-message title (legacy `user_message` event, paginated `item_completed` UserMessage, or a user-role `response_item`); tail derivation keying attention on the last of `turn_started`/`turn_complete`/`turn_aborted` (persisted in *both* history modes) with edited files from `patch_apply_end.changes` (legacy) and `item_completed` FileChange items (paginated). Unknown record/event types are ignored silently at both nesting levels — churn resilience against Codex's ~daily schema drift is the design requirement. Synthetic fixtures authored from the researched schema (plan Appendix A, rust-v0.144.1); no codex binary on the build box.
- ✓ pi read path — discovery + session parser (WP4): the depth-2 `--<encoded-cwd>--/<ts>_<id>.jsonl` predicate; id derivation as the substring after the *first* `_` (validated against pi's `[A-Za-z0-9._-]` grammar, not assumed uuid-shaped); head parse of the v3 `session` header cwd + a title from the latest `session_info` name (last rename wins) falling back to the first user message; tail derivation mapping the last `message` entry's assistant `stopReason` (`toolUse` → Working, else NeedsInput) with edited files from `write`/`edit` `toolCall` blocks (`arguments.path`, tolerating the `file_path` alias), resolved against the header cwd. The tree structure (`id`/`parentId`) is read *linearly*; branch-awareness is out of scope. `default_transcript_root` reads `PI_CODING_AGENT_SESSION_DIR`/`PI_CODING_AGENT_DIR` as local-only overrides (config still wins). Synthetic fixtures (plan Appendix B, @earendil-works/pi-coding-agent v0.80.6); no pi binary on the build box.
- ✓ codex spawn via post-launch adoption + resume (WP5): codex can't pin a session id (upstream declined `--session-id`), so `SpawnPlan::DiscoverAfterSpawn` replaces the pinned-id identity contract. `spawn_session` (threaded a new `AgentKind` param, `AttachmentDriver` + both drivers) launches the bare `codex` binary under a *provisional* `agent-mux-pending-<nonce>` tmux name; `drain_creates` records a pending-spawn entry (new pure `adoption.rs` module — `PendingSpawns` table, `ADOPTION_WINDOW` = 30s, FIFO on same-cwd collisions). When the rollout the watcher is already tailing appears in that cwd, the `NewTranscript` handler — keying off the *raw* event, ahead of discovery's stillborn filter so a `session_meta`-only rollout still correlates — reads its cwd, adopts the entry, renames the session to `agent-mux-<rollout-id>` through the driver's new `adopt_pending_session` (tmux stays behind the driver; remote routes via `ssh_argv`), and re-keys the embedded pane from the spawn placeholder to the real id. A pending spawn whose rollout never appears expires into a footer error, swept on each tick + transcript. Resume reuses the existing `tmux_resume_argv` machinery parameterised by the agent's `resume_command` (`codex resume <id>`). The `n`/`N` flow gained an `agent` field on `NewSessionResult::Created` wired to `spawn_session`; WP7's picker sets it, making the codex path UI-reachable. No `codex` binary on the build box, so end-to-end is unit-tested only (adoption state machine, spawn/rename/resume argv incl. ssh-quoting, adopted-session re-attach); not verified live against a real codex.
- ✓ pi spawn/resume via pinned session id (WP6): pi accepts a caller-chosen id (`pi --session-id <id>` — creates if missing, reopens if present), so it flows through the existing `SpawnPlan::PinnedId` arm unchanged — the minted uuid is simultaneously the `agent-mux-<uuid>` tmux name and the `--session-id`, and `session_id_from_path` bridges pi's `<ts>_<id>.jsonl` filename to that same id (today's identity contract, no adoption). Spawn *and* resume are the same flag (the direct `claude --session-id` analog); `resume_command` = `pi --session-id <id>` reuses the shared `tmux_resume_argv` machinery. Near-zero attachment.rs change (WP5 already generalised `spawn_session` to `AgentKind`); the work was finalising the pi stubs and pinning tests mirroring claude/codex coverage — local spawn argv shape (uuid as both `--session-id` and tmux name), resume-fallback argv, remote-resume ssh-quoting (the multi-word `pi --session-id <id>` survives as one quoted unit), and pinned re-attach by name. No `pi` binary on the build box, so end-to-end is unit-tested only. Note (deferred to WP9): `AgentCli::spawn`/`resume_command` hardcode the literal binary name for *all* agents — the `[agents.<label>] binary` override (`Config::agent_binary`) is defined but not yet consumed by any spawn/resume path.
- ✓ agent tag on rows + new-session agent picker (WP7): the UI surface for multi-agent, both behind a single **≥2-enabled-agents gate** so the Claude-only dashboard is byte-identical to pre-WP7 (proven by the existing snapshot/`build_display_rows` tests passing unmodified). (1) Each session row's line-2 status line ends with a dim agent label (`claude`/`codex`/`pi`, the bare `AgentKind::label()`) carried on the same muted `detail` span as the durations — placed *after* the `· <n> old` cell so the attention glyph/word keeps its leading signal position (altitude rule); `show_agent_tags` (`config.enabled_agents().len() >= 2`) threads through `build_sidebar_items` → `format_session_row`. (2) The quickswitcher rows get the same dim ` · <label>` context cell under the same gate, and the label joins the fuzzy haystack so typing "codex" filters to codex sessions (both gated so a single-agent haystack can't let a stray "cl…" match every row). (3) The `n`/`N` flow gains a `NewSessionModal::PickingAgent` step between the repo pick and the terminal action when ≥2 agents are enabled (list of enabled agents, first = preselected default, same list-picker idiom + keybinds as the repo/branch steps; Esc cancels the whole modal, matching the `Filling` step — no step-back); a single enabled agent skips it entirely (zero extra keystroke). The chosen `AgentKind` flows `KeyOutcome::Submit`/`SubmitNoWorktree` → `start_creating`/`start_no_worktree_session` → `NewSessionResult::Created.agent` → `spawn_session`, so the codex/pi spawn machinery WP5/WP6 wired is now UI-reachable. Also de-Claude-ified the `cli.rs` banner ("terminal multiplexer for agent CLI sessions (Claude Code, Codex, Pi)") and the `transcript_root`/`[agents]` config-reference help (hook-installer strings left for WP8/WP9). Tests: both gates on `format_session_row` (single-agent → no tag, multi-agent → tag at line end after durations, attention word precedes it), and the modal state machine (single-agent skips the step in both flows; ≥2 interposes it, preselects the default, threads the picked kind into Filling→Submit and into SubmitNoWorktree, wraps on nav, Esc-cancels).
- ✓ codex hook parity — `PermissionRequest`/`Stop` ingest + installer (WP8): the marker-file consumer (hook watcher, `poll_hooks_once`, catalog `apply_hook_event` pin) was already agent-neutral, so codex parity is a second **producer** and a second **installer**, with the consumer untouched. (1) **Producer** `agent-mux hook --agent codex` reads Codex's lifecycle-hook JSON from stdin (`session_id`, `cwd`, `hook_event_name`, …) and normalises into the *existing* claude marker vocabulary — `PermissionRequest` → a `permission_prompt` (blocking) marker, `Stop` → an `idle_prompt` (turn-complete, non-blocking) marker — so no marker-schema change was needed; routing stays by `session_id` (= the codex rollout/thread uuid). Codex payloads carry no `transcript_path`, so the marker dir is resolved from the codex transcript root (config override → agent default → `~/.codex/sessions/.agent-mux-hooks/`), exactly what the consumer drains. (2) **Consumer** the single local hook watcher now watches every enabled agent's `<root>/.agent-mux-hooks/` under one `notify` backend (mirroring WP2's one-transcript-watcher discipline; the recursive transcript watch already *sees* these dirs but filters their `.json` markers by extension, so this dedicated watcher owns them); the remote poller ticks each enabled agent's hook dir per SSH `ControlMaster`. (3) **Installer** `agent-mux install-hooks --agent codex` merges the two `type:"command"` handlers into `~/.codex/hooks.json` (best-effort schema mirrored on Claude Code's proven layout — a correction is localised to one function), idempotent + `--dry-run` + stale-path update in place, reusing the `is_agent_mux_hook_command` recognition (the trailing `--agent codex` doesn't change the first two tokens); default `install-hooks` stays claude-only. (4) **Clobber fix** codex approvals never persist to the rollout, so the hook is codex's *only* blocked signal — and a `PermissionRequest` pin would be cleared by every open-turn `Working` poll (which WP3 derived with `from_tool_use=false`). Codex's open-turn derive now reports `from_tool_use=true` (semantically claude's `tool_use` wait), so the catalog's discriminator protects the pin; a `turn_complete` (NeedsInput, `from_tool_use=false`) is genuine progress and releases it. No `codex` binary on the build box, so end-to-end is unit-tested only: producer stdin→marker (blocking + non-blocking + unknown-skip + missing-id), installer merge/idempotency/dry-run/recognition across both event arrays, the catalog clobber regression, and consumer routing (codex marker → blocking pin for the right session via the unchanged parse). `#agents #hooks #attention #codex`
